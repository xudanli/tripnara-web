# 规划工作台页面未对接接口清单

**生成时间**: 2025-01-XX  
**页面路径**: `src/pages/plan-studio/`  
**统计**: 共 **12** 个已提供但未对接的接口

---

## 📋 目录

1. [已提供但未对接的接口](#1-已提供但未对接的接口)
2. [接口使用位置分析](#2-接口使用位置分析)
3. [对接优先级建议](#3-对接优先级建议)

---

## 1. 已提供但未对接的接口

### 1.1 行程优化相关

#### 1.1.1 优化路线接口

| 接口路径 | 方法 | API文件 | 页面位置 | 状态 |
|---------|------|---------|---------|------|
| `/itinerary-optimization/optimize` | POST | `src/api/itinerary-optimization.ts` | `OptimizeTab.tsx` (第23行有TODO) | ⚠️ 未对接 |

**说明**: 
- 接口已定义在 `itineraryOptimizationApi.optimize`
- `OptimizeTab.tsx` 中有 TODO 注释：`// TODO: 调用优化API`
- `ScheduleTab.tsx` 第573行也有 TODO：`// TODO: 调用优化 API`

**使用场景**:
- OptimizeTab: 点击"生成计划"按钮时调用
- ScheduleTab: 点击"Run Optimize"按钮时调用

---

#### 1.1.2 自然语言转参数接口

| 接口路径 | 方法 | API文件 | 页面位置 | 状态 |
|---------|------|---------|---------|------|
| `/llm/natural-language-to-params` | POST | `src/api/itinerary-optimization.ts` | 未使用 | ⚠️ 未对接 |

**说明**: 
- 接口已定义在 `itineraryOptimizationApi.naturalLanguageToParams`
- 可用于优化参数的智能输入

---

#### 1.1.3 结果人性化转化接口

| 接口路径 | 方法 | API文件 | 页面位置 | 状态 |
|---------|------|---------|---------|------|
| `/llm/humanize-result` | POST | `src/api/itinerary-optimization.ts` | 未使用 | ⚠️ 未对接 |

**说明**: 
- 接口已定义在 `itineraryOptimizationApi.humanizeResult`
- 可用于优化结果的友好展示

---

### 1.2 规划策略相关

#### 1.2.1 What-If 评估接口

| 接口路径 | 方法 | API文件 | 页面位置 | 状态 |
|---------|------|---------|---------|------|
| `/planning-policy/what-if/evaluate` | POST | `src/api/planning-policy.ts` | `WhatIfTab.tsx` (第23行有TODO) | ⚠️ 未对接 |

**说明**: 
- 接口已定义在 `planningPolicyApi.whatIfEvaluate`
- `WhatIfTab.tsx` 中有 TODO 注释：`// TODO: 调用What-If API`
- 当前使用硬编码的模拟数据

**使用场景**:
- WhatIfTab: 点击"生成对比方案"按钮时调用

---

#### 1.2.2 其他规划策略接口

以下接口已提供但未在规划工作台使用：

| 接口路径 | 方法 | API文件 | 状态 |
|---------|------|---------|------|
| `/planning-policy/robustness/evaluate-day` | POST | `src/api/planning-policy.ts` | ⚠️ 未对接 |
| `/planning-policy/what-if/generate-candidates` | POST | `src/api/planning-policy.ts` | ⚠️ 未对接 |
| `/planning-policy/what-if/evaluate-candidates` | POST | `src/api/planning-policy.ts` | ⚠️ 未对接 |
| `/planning-policy/what-if/apply` | POST | `src/api/planning-policy.ts` | ⚠️ 未对接 |
| `/planning-policy/what-if/re-evaluate` | POST | `src/api/planning-policy.ts` | ⚠️ 未对接 |

**说明**: 
- 这些接口可用于更细粒度的 What-If 分析流程
- 当前 WhatIfTab 只使用了硬编码数据

---

### 1.3 决策引擎相关

#### 1.3.1 安全规则校验（Abu策略）

| 接口路径 | 方法 | API文件 | 页面位置 | 状态 |
|---------|------|---------|---------|------|
| `/decision/validate-safety` | POST | `src/api/decision.ts` | `PlanStudioSidebar.tsx` (Abu视图) | ⚠️ 未对接 |

**说明**: 
- 接口已定义在 `decisionApi.validateSafety`
- `PlanStudioSidebar.tsx` 中 Abu 视图使用硬编码数据
- 可用于门控状态检查

---

#### 1.3.2 行程节奏智能调整（Dr.Dre策略）

| 接口路径 | 方法 | API文件 | 页面位置 | 状态 |
|---------|------|---------|---------|------|
| `/decision/adjust-pacing` | POST | `src/api/decision.ts` | `PlanStudioSidebar.tsx` (Dr.Dre视图) | ⚠️ 未对接 |

**说明**: 
- 接口已定义在 `decisionApi.adjustPacing`
- `PlanStudioSidebar.tsx` 中 Dr.Dre 视图使用硬编码数据
- 可用于节奏优化

---

#### 1.3.3 路线节点智能替换（Neptune策略）

| 接口路径 | 方法 | API文件 | 页面位置 | 状态 |
|---------|------|---------|---------|------|
| `/decision/replace-nodes` | POST | `src/api/decision.ts` | `PlanStudioSidebar.tsx` (Neptune视图) | ⚠️ 未对接 |

**说明**: 
- 接口已定义在 `decisionApi.replaceNodes`
- `PlanStudioSidebar.tsx` 中 Neptune 视图使用硬编码数据
- 可用于节点替换修复

---

### 1.4 行程管理相关

#### 1.4.1 行程调整接口

| 接口路径 | 方法 | API文件 | 页面位置 | 状态 |
|---------|------|---------|---------|------|
| `/trips/:id/adjust` | POST | `src/api/trips.ts` | 未使用 | ⚠️ 未对接 |

**说明**: 
- 接口已定义在 `tripsApi.adjust`
- 可用于 ScheduleTab 中的行程调整功能
- `ScheduleTab.tsx` 第579行有 TODO：`// TODO: 自动添加缓冲`

---

#### 1.4.2 三人格提醒接口

| 接口路径 | 方法 | API文件 | 页面位置 | 状态 |
|---------|------|---------|---------|------|
| `/trips/:id/persona-alerts` | GET | `src/api/trips.ts` | `PlanStudioSidebar.tsx` | ⚠️ 未对接 |

**说明**: 
- 接口已定义在 `tripsApi.getPersonaAlerts`
- `PlanStudioSidebar.tsx` 中三个视图的提示卡都使用硬编码数据
- 可用于显示真实的提醒信息

---

#### 1.4.3 决策日志接口

| 接口路径 | 方法 | API文件 | 页面位置 | 状态 |
|---------|------|---------|---------|------|
| `/trips/:id/decision-log` | GET | `src/api/trips.ts` | 未使用 | ⚠️ 未对接 |

**说明**: 
- 接口已定义在 `tripsApi.getDecisionLog`
- 可用于显示决策透明日志
- 可能需要在 PlanStudioSidebar 或新的标签页中使用

---

### 1.5 预算管理相关

以下预算相关接口已提供但未在规划工作台使用：

| 接口路径 | 方法 | API文件 | 状态 |
|---------|------|---------|------|
| `/trips/:id/budget/summary` | GET | `src/api/trips.ts` | ⚠️ 未对接 |
| `/trips/:id/budget/alert` | GET | `src/api/trips.ts` | ⚠️ 未对接 |
| `/trips/:id/budget/optimization` | GET | `src/api/trips.ts` | ⚠️ 未对接 |
| `/trips/:id/budget/report` | GET | `src/api/trips.ts` | ⚠️ 未对接 |

**说明**: 
- 这些接口可用于预算相关的功能展示
- 当前规划工作台没有预算相关的标签页或组件

---

### 1.6 其他已提供但未使用的接口

#### 1.6.1 替换行程项接口

| 接口路径 | 方法 | API文件 | 页面位置 | 状态 |
|---------|------|---------|---------|------|
| `/trips/:tripId/items/:itemId/replace` | POST | `src/api/trips.ts` | `ScheduleTab.tsx` | ⚠️ 部分对接 |

**说明**: 
- 接口已定义在 `tripsApi.replaceItem`
- `ScheduleTab.tsx` 中使用了 `ReplaceItineraryItemDialog` 组件
- 但该组件可能使用的是其他接口，需要确认

---

#### 1.6.2 重生成行程接口

| 接口路径 | 方法 | API文件 | 页面位置 | 状态 |
|---------|------|---------|---------|------|
| `/trips/:tripId/regenerate` | POST | `src/api/trips.ts` | 未使用 | ⚠️ 未对接 |

**说明**: 
- 接口已定义在 `tripsApi.regenerate`
- 可用于全局重生成行程功能

---

## 2. 接口使用位置分析

### 2.1 ScheduleTab.tsx

**已对接的接口**:
- ✅ `tripsApi.getById` - 获取行程详情
- ✅ `tripsApi.getSchedule` - 获取日程安排
- ✅ `itineraryItemsApi.getAll` - 获取行程项列表
- ✅ `itineraryItemsApi.getById` - 获取行程项详情
- ✅ `itineraryItemsApi.update` - 更新行程项
- ✅ `itineraryItemsApi.delete` - 删除行程项

**未对接的接口**:
- ⚠️ `itineraryOptimizationApi.optimize` - 第573行 TODO
- ⚠️ 自动添加缓冲功能 - 第579行 TODO
- ⚠️ `tripsApi.adjust` - 可用于行程调整

**硬编码数据**:
- 第349-355行：每日指标（walk, drive, buffer, conflicts）使用硬编码
- 第516-527行：指标面板使用硬编码数据
- 第532-567行：冲突列表使用硬编码数据

---

### 2.2 IntentTab.tsx

**已对接的接口**:
- ✅ `tripsApi.getById` - 获取行程详情
- ✅ `placesApi.autocompletePlaces` - 地点自动补全

**未对接的接口**:
- ⚠️ 保存意图与约束接口 - 第168行 TODO：`// TODO: 调用API保存意图与约束`
- ⚠️ 从 trip 数据加载已有设置 - 第89行 TODO：`// TODO: 从trip.preferences或planningPolicy中读取`

**说明**: 
- 当前保存功能只是打印到控制台，没有实际调用API
- 需要确认是否有对应的更新接口

---

### 2.3 OptimizeTab.tsx

**已对接的接口**:
- 无

**未对接的接口**:
- ⚠️ `itineraryOptimizationApi.optimize` - 第23行 TODO
- ⚠️ 其他优化相关接口

**说明**: 
- 当前完全使用硬编码的模拟数据
- 需要对接优化接口来获取真实结果

---

### 2.4 WhatIfTab.tsx

**已对接的接口**:
- 无

**未对接的接口**:
- ⚠️ `planningPolicyApi.whatIfEvaluate` - 第23行 TODO
- ⚠️ 其他 What-If 相关接口

**说明**: 
- 当前完全使用硬编码的模拟数据
- 需要对接 What-If 评估接口

---

### 2.5 PlanStudioSidebar.tsx

**已对接的接口**:
- 无

**未对接的接口**:
- ⚠️ `tripsApi.getPersonaAlerts` - 获取三人格提醒
- ⚠️ `decisionApi.validateSafety` - Abu 策略校验
- ⚠️ `decisionApi.adjustPacing` - Dr.Dre 策略调整
- ⚠️ `decisionApi.replaceNodes` - Neptune 策略替换

**硬编码数据**:
- 第14-18行：Abu 视图门控状态和违规数量
- 第20-31行：Dr.Dre 视图指标数据
- 第33-40行：Neptune 视图修复数据
- 第192-277行：三个视图的提示卡都使用硬编码数据

**说明**: 
- 整个组件都使用硬编码数据，需要对接多个接口来获取真实数据

---

## 3. 对接优先级建议

### P0 - 高优先级（核心功能）

1. **`POST /itinerary-optimization/optimize`** 
   - 位置: `OptimizeTab.tsx`, `ScheduleTab.tsx`
   - 影响: 优化功能无法使用

2. **`POST /planning-policy/what-if/evaluate`**
   - 位置: `WhatIfTab.tsx`
   - 影响: What-If 分析功能无法使用

3. **`GET /trips/:id/persona-alerts`**
   - 位置: `PlanStudioSidebar.tsx`
   - 影响: 右侧栏提示信息不准确

### P1 - 中优先级（重要功能）

4. **`POST /decision/validate-safety`** (Abu策略)
   - 位置: `PlanStudioSidebar.tsx`
   - 影响: Abu 视图门控状态不准确

5. **`POST /decision/adjust-pacing`** (Dr.Dre策略)
   - 位置: `PlanStudioSidebar.tsx`
   - 影响: Dr.Dre 视图指标不准确

6. **`POST /decision/replace-nodes`** (Neptune策略)
   - 位置: `PlanStudioSidebar.tsx`
   - 影响: Neptune 视图修复建议不准确

7. **IntentTab 保存接口**
   - 位置: `IntentTab.tsx`
   - 影响: 意图与约束无法保存

### P2 - 低优先级（增强功能）

8. **`GET /trips/:id/decision-log`**
   - 位置: 可能需要新组件
   - 影响: 决策透明日志无法查看

9. **预算相关接口**
   - 位置: 可能需要新组件
   - 影响: 预算功能缺失

10. **`POST /trips/:id/adjust`**
    - 位置: `ScheduleTab.tsx`
    - 影响: 自动调整功能缺失

---

## 4. 总结

### 统计信息

- **已对接接口**: 约 8 个（主要在 ScheduleTab 和 PlacesTab）
- **未对接接口**: 12 个核心接口 + 多个辅助接口
- **硬编码数据位置**: 
  - `ScheduleTab.tsx`: 3 处
  - `PlanStudioSidebar.tsx`: 多处（整个组件）
  - `OptimizeTab.tsx`: 完全硬编码
  - `WhatIfTab.tsx`: 完全硬编码

### 主要问题

1. **OptimizeTab 和 WhatIfTab 完全使用硬编码数据**
2. **PlanStudioSidebar 所有数据都是硬编码**
3. **IntentTab 保存功能未实现**
4. **ScheduleTab 的优化和缓冲功能未实现**

### 建议

1. 优先对接 P0 级别的接口，确保核心功能可用
2. 逐步替换硬编码数据，先从 PlanStudioSidebar 开始
3. 完善 IntentTab 的保存功能
4. 考虑添加预算相关的标签页或组件

---

## 5. 注意事项

1. **接口已提供**: 本文档列出的接口都在 `src/api/` 目录下已定义
2. **后端状态**: 需要确认这些接口在后端是否已实现
3. **类型定义**: 大部分接口的类型定义在 `src/types/` 目录下
4. **错误处理**: 对接时需要添加适当的错误处理和加载状态

---

**最后更新**: 2025-01-XX


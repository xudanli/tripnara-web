/** Tailwind `xl` — 与 PlanningWorkbenchLayout 三栏断点一致 */
export const WORKBENCH_DESKTOP_BREAKPOINT_PX = 1280;

export type WorkbenchMobileColumn =
  | 'summary'
  | 'constraints'
  | 'itinerary'
  | 'decision';

/** 分支画卷 — 本地 public 场景（/vine-scenes/*.svg） */
const BASE = '/vine-scenes';

export const VINE_SCENE_IMAGES = {
  restoration: `${BASE}/restoration.svg`,
  slow_travel: `${BASE}/slow_travel.svg`,
  wilderness: `${BASE}/wilderness.svg`,
  resonance: `${BASE}/resonance.svg`,
  soil: `${BASE}/soil.svg`,
} as const;

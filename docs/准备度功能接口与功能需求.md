# 准备度功能接口与功能需求

**文档版本**: v1.0  
**创建时间**: 2026-01-26  
**负责人**: 产品经理（Danny）

---

## 📋 一、功能定位与业务价值

### 1.1 功能定位

**准备度（Readiness）**是 TripNARA 决策型旅行应用中的**核心门控功能**，位于"规划工作台"之后、"执行阶段"之前，是确保行程可执行性的关键环节。

**核心价值**：
- **决策门控**：判断行程是否"应该存在"（Should-Exist Gate）
- **风险识别**：提前识别并预警潜在风险
- **可执行性保障**：确保行程具备可执行的基础条件（签证、保险、装备等）
- **个性化准备**：基于用户画像和行程特征，生成个性化准备清单

### 1.2 在用户旅程中的位置

```
创建行程 → 规划工作台 → [准备度评估] → 打包清单 → 执行阶段 → 复盘
```

**关键决策点**：
- 如果准备度不达标（存在阻塞项），**不允许进入执行阶段**
- 如果准备度达标，用户可以继续准备（打包清单）或直接进入执行阶段

---

## 🎯 二、核心功能模块

### 2.1 准备度检查（Readiness Check）

**业务目标**：全面评估行程的准备度，识别阻塞项、必须项、建议项和可选项。

**功能特性**：
- **多维度评估**：签证、保险、安全、健康、交通、住宿等
- **分级管理**：blocker（阻塞）/ must（必须）/ should（建议）/ optional（可选）
- **证据链支持**：每个检查项关联证据来源，支持可解释性
- **多目的地支持**：支持多国行程的分别评估

### 2.2 风险预警（Risk Warnings）

**业务目标**：识别并预警行程中的潜在风险，提供应对措施。

**功能特性**：
- **风险分类**：天气、地形、海拔、安全、健康等
- **严重程度分级**：high / medium / low
- **应对措施**：为每个风险提供具体的应对建议
- **紧急联系**：高风险场景提供紧急联系方式

### 2.3 个性化准备清单（Personalized Checklist）

**业务目标**：基于用户画像和行程特征，生成个性化的准备清单。

**功能特性**：
- **个性化推荐**：基于用户国籍、预算水平、风险承受度等
- **任务时间线**：每个任务关联截止日期（相对出发日期）
- **多语言支持**：支持中英文切换
- **动态更新**：行程变更时自动更新清单

### 2.4 能力包评估（Capability Packs）

**业务目标**：评估行程触发了哪些系统能力包，提供专项准备建议。

**功能特性**：
- **能力包识别**：自动识别行程触发的特殊场景（如极光、徒步、自驾等）
- **专项建议**：为每个能力包提供针对性的准备建议
- **触发条件**：基于目的地、活动类型、季节等条件触发

### 2.5 打包清单（Packing List）

**业务目标**：生成个性化的打包清单，帮助用户准备行李。

**功能特性**：
- **模板模式**：支持基于模板生成（如冰岛专用模板）
- **参数推断**：自动推断季节、路线类型、用户类型、活动类型
- **分类管理**：按分类（衣物、电子、洗漱、证件等）组织清单
- **优先级标识**：must / should / optional 优先级
- **状态管理**：支持勾选、编辑数量、添加备注

### 2.6 修复方案（Repair Options）

**业务目标**：为阻塞项提供修复方案，帮助用户解决准备度问题。

**功能特性**：
- **方案推荐**：为每个阻塞项推荐多个修复方案
- **方案评估**：评估每个方案的成本、时间、可行性
- **自动修复**：支持一键应用修复方案（Neptune 自动修复）

---

## 🔌 三、接口清单与功能说明

### 3.1 核心检查接口

#### 3.1.1 获取行程准备度（主要接口）

**接口**：`GET /readiness/trip/:tripId?lang=zh`

**功能**：根据行程ID获取完整的准备度评估结果。

**业务场景**：
- 用户进入准备度页面时调用
- 行程信息变更后刷新准备度
- 准备度状态监控

**返回数据**：
- `findings`: 多个目的地的检查结果（支持多国行程）
- `summary`: 统计摘要（阻塞项、必须项、建议项、可选项数量）
- `risks`: 风险列表
- `constraints`: 约束列表

**关键字段**：
- `ReadinessFinding.category`: 分类（entry, safety, health, transport, lodging 等）
- `ReadinessFindingItem.level`: 级别（blocker / must / should / optional）
- `ReadinessFindingItem.tasks`: 任务列表（带截止日期）
- `ReadinessFindingItem.evidence`: 证据引用

**参考实现**：`src/api/readiness.ts` - `getTripReadiness()`

---

#### 3.1.2 检查准备度（备用接口）

**接口**：`POST /readiness/check`

**功能**：基于自定义参数检查准备度（不依赖行程ID）。

**业务场景**：
- 行程创建前的预检查
- 多目的地组合检查
- 自定义场景测试

**请求参数**：`CheckReadinessDto`
- `destinationId`: 目的地代码
- `traveler`: 旅行者信息（国籍、居住国、标签、预算水平、风险承受度）
- `trip`: 行程信息（开始日期、结束日期）
- `itinerary`: 行程详情（国家列表、活动、季节、区域、车辆类型等）
- `geo`: 地理位置信息（可选，用于增强检查）

**参考实现**：`src/api/readiness.ts` - `check()`

---

### 3.2 个性化清单接口

#### 3.2.1 获取个性化准备清单

**接口**：`GET /readiness/personalized-checklist?tripId=xxx&lang=zh`

**功能**：获取基于用户画像和行程特征的个性化准备清单。

**业务场景**：
- 准备度页面"准备清单"标签页
- 用户查看个性化建议
- 任务时间线展示

**返回数据**：
- `checklist`: 分级清单（blocker / must / should / optional）
- `summary`: 统计摘要
- `tasks`: 任务列表（带截止日期）

**关键特性**：
- **多语言支持**：支持 `lang` 参数（zh / en）
- **个性化推荐**：基于用户画像（国籍、预算、风险承受度）推荐
- **任务时间线**：每个任务关联截止日期（相对出发日期）

**参考实现**：`src/api/readiness.ts` - `getPersonalizedChecklist()`

---

### 3.3 风险预警接口

#### 3.3.1 获取风险预警

**接口**：`GET /readiness/risk-warnings?tripId=xxx&lang=zh`

**功能**：获取行程的潜在风险预警列表。

**业务场景**：
- 准备度页面"风险预警"区域
- 风险详情查看
- 风险应对措施展示

**返回数据**：
- `risks`: 风险列表
- `summary`: 风险统计（总数、高/中/低严重程度数量）

**风险结构**：
- `type`: 风险类型（altitude, terrain, weather, safety, health 等）
- `severity`: 严重程度（high / medium / low）
- `message`: 风险描述
- `mitigations`: 应对措施列表
- `emergencyContacts`: 紧急联系方式（高风险场景）

**参考实现**：`src/api/readiness.ts` - `getRiskWarnings()`

---

### 3.4 能力包接口

#### 3.4.1 获取能力包列表

**接口**：`GET /readiness/capability-packs`

**功能**：获取所有可用的系统能力包列表。

**业务场景**：
- 准备度页面"能力包"标签页
- 能力包详情查看

**返回数据**：
- `packs`: 能力包列表

**能力包结构**：
- `id`: 能力包ID（如 `pack.is.iceland`）
- `name`: 能力包名称
- `description`: 描述
- `triggerConditions`: 触发条件

**参考实现**：`src/api/readiness.ts` - `getCapabilityPacks()`

---

#### 3.4.2 评估能力包

**接口**：`POST /readiness/capability-packs/evaluate`

**功能**：评估行程触发了哪些能力包。

**业务场景**：
- 准备度页面"能力包"标签页
- 显示触发的能力包和评估结果

**请求参数**：`CheckReadinessDto`（与检查准备度相同）

**返回数据**：
- `total`: 总能力包数
- `triggered`: 触发的能力包数
- `results`: 评估结果列表（每个能力包的触发状态和评估详情）

**参考实现**：`src/api/readiness.ts` - `evaluateCapabilityPacks()`

---

### 3.5 打包清单接口

#### 3.5.1 生成打包清单

**接口**：`POST /readiness/trip/:tripId/packing-list/generate`

**功能**：生成个性化的打包清单。

**业务场景**：
- 准备度页面"打包清单"标签页
- 用户点击"生成清单"按钮

**请求参数**：
- `useTemplate`: 是否使用模板模式（boolean）
- `season`: 季节（summer / transition / winter）
- `route`: 路线类型（golden_circle / south_coast / full_ring_road 等）
- `userType`: 用户类型（first_timer / photographer / adventurer 等）
- `activities`: 活动列表（string[]）
- `vehicleType`: 车辆类型（可选）
- `specialNeeds`: 特殊需求（可选）
- `includeOptional`: 是否包含可选项（boolean）
- `categories`: 指定分类（可选）
- `customItems`: 自定义物品（可选）

**返回数据**：
- `tripId`: 行程ID
- `generatedAt`: 生成时间
- `items`: 清单项列表
- `summary`: 统计摘要（总项数、按分类统计）

**清单项结构**：
- `id`: 物品ID
- `name`: 物品名称
- `category`: 分类（clothing, electronics, toiletries, documents 等）
- `quantity`: 数量
- `unit`: 单位（件、个等）
- `priority`: 优先级（must / should / optional）
- `reason`: 推荐原因
- `sourceFindingId`: 来源检查项ID（可选）
- `checked`: 是否已勾选
- `note`: 备注（可选）

**参数推断**：
- 前端自动推断参数（季节、路线、用户类型、活动）
- 参考实现：`src/utils/packing-list-inference.ts`

**参考实现**：`src/api/readiness.ts` - `generatePackingList()`

---

#### 3.5.2 获取打包清单

**接口**：`GET /readiness/trip/:tripId/packing-list`

**功能**：获取已生成的打包清单。

**业务场景**：
- 准备度页面"打包清单"标签页加载时
- 刷新打包清单

**返回数据**：
- `tripId`: 行程ID
- `items`: 清单项列表
- `summary`: 统计摘要（总项数、已勾选数、按分类统计）
- `lastGeneratedAt`: 最后生成时间

**参考实现**：`src/api/readiness.ts` - `getPackingList()`

---

#### 3.5.3 更新打包清单项

**接口**：`PUT /readiness/trip/:tripId/packing-list/items/:itemId`

**功能**：更新打包清单项的状态（勾选、数量、备注）。

**业务场景**：
- 用户勾选/取消勾选物品
- 用户编辑数量
- 用户添加备注

**请求参数**：
- `checked`: 是否已勾选（boolean，可选）
- `quantity`: 数量（number，可选）
- `note`: 备注（string，可选）

**返回数据**：
- `itemId`: 物品ID
- `updated`: 是否更新成功

**参考实现**：`src/api/readiness.ts` - `updatePackingListItem()`

---

### 3.6 修复方案接口

#### 3.6.1 获取阻塞项解决方案

**接口**：`GET /readiness/trip/:tripId/blockers/:blockerId/solutions`

**功能**：获取阻塞项的修复方案列表。

**业务场景**：
- 用户点击阻塞项的"查看解决方案"按钮
- 修复方案对话框展示

**返回数据**：
- `blockerId`: 阻塞项ID
- `blockerMessage`: 阻塞项消息
- `solutions`: 解决方案列表

**解决方案结构**：
- `id`: 方案ID
- `title`: 方案标题
- `description`: 方案描述
- `cost`: 成本（可选）
- `timeRequired`: 所需时间（可选）
- `feasibility`: 可行性评分（可选）

**参考实现**：`src/api/readiness.ts` - `getSolutions()`

---

#### 3.6.2 应用修复方案

**接口**：`POST /readiness/apply-repair`

**功能**：应用修复方案，解决阻塞项。

**业务场景**：
- 用户选择修复方案并点击"应用"
- 自动修复流程

**请求参数**：
- `tripId`: 行程ID
- `blockerId`: 阻塞项ID
- `optionId`: 修复方案ID

**返回数据**：
- `success`: 是否成功
- `message`: 消息（可选）

**参考实现**：`src/api/readiness.ts` - `applyRepair()`

---

#### 3.6.3 自动修复（Neptune）

**接口**：`POST /readiness/auto-repair`

**功能**：Neptune 自动修复所有阻塞项。

**业务场景**：
- 用户点击"自动修复"按钮
- Neptune 智能修复流程

**请求参数**：
- `tripId`: 行程ID

**返回数据**：
- `success`: 是否成功
- `message`: 消息（可选）

**参考实现**：`src/api/readiness.ts` - `autoRepair()`

---

### 3.7 状态管理接口

#### 3.7.1 获取勾选状态

**接口**：`GET /readiness/trip/:tripId/checklist/status`

**功能**：获取准备清单的勾选状态。

**业务场景**：
- 准备度页面加载时恢复勾选状态
- 跨设备同步勾选状态

**返回数据**：
- `checkedItems`: 已勾选的检查项ID列表
- `lastUpdated`: 最后更新时间

**参考实现**：`src/api/readiness.ts` - `getChecklistStatus()`

---

#### 3.7.2 更新勾选状态

**接口**：`PUT /readiness/trip/:tripId/checklist/status`

**功能**：批量更新准备清单的勾选状态。

**业务场景**：
- 用户勾选/取消勾选检查项
- 批量操作

**请求参数**：
- `checkedItems`: 已勾选的检查项ID列表

**返回数据**：
- `updated`: 更新的项数
- `checkedItems`: 更新后的已勾选列表

**参考实现**：`src/api/readiness.ts` - `updateChecklistStatus()`

---

#### 3.7.3 标记为不适用

**接口**：`POST /readiness/trip/:tripId/findings/:findingId/mark-not-applicable`

**功能**：标记检查项为"不适用"（用户认为该检查项不适用于自己的情况）。

**业务场景**：
- 用户认为某个检查项不适用
- 个性化过滤

**请求参数**：
- `reason`: 原因（可选）

**返回数据**：
- `findingId`: 检查项ID
- `marked`: 是否已标记
- `reason`: 原因（可选）
- `markedAt`: 标记时间

**参考实现**：`src/api/readiness.ts` - `markNotApplicable()`

---

#### 3.7.4 取消标记不适用

**接口**：`DELETE /readiness/trip/:tripId/findings/:findingId/mark-not-applicable`

**功能**：取消"不适用"标记。

**业务场景**：
- 用户撤销"不适用"标记

**返回数据**：
- `findingId`: 检查项ID
- `marked`: 是否已标记（false）

**参考实现**：`src/api/readiness.ts` - `unmarkNotApplicable()`

---

#### 3.7.5 添加到稍后处理

**接口**：`POST /readiness/trip/:tripId/findings/:findingId/add-to-later`

**功能**：将检查项添加到"稍后处理"列表。

**业务场景**：
- 用户暂时不想处理某个检查项
- 稍后提醒功能

**请求参数**：
- `reminderDate`: 提醒日期（可选）
- `note`: 备注（可选）

**返回数据**：
- `findingId`: 检查项ID
- `added`: 是否已添加
- `reminderDate`: 提醒日期（可选）
- `note`: 备注（可选）
- `addedAt`: 添加时间

**参考实现**：`src/api/readiness.ts` - `addToLater()`

---

#### 3.7.6 从稍后处理移除

**接口**：`DELETE /readiness/trip/:tripId/findings/:findingId/remove-from-later`

**功能**：从"稍后处理"列表移除检查项。

**业务场景**：
- 用户处理完检查项后移除

**返回数据**：
- `findingId`: 检查项ID
- `removed`: 是否已移除

**参考实现**：`src/api/readiness.ts` - `removeFromLater()`

---

#### 3.7.7 获取不适用项列表

**接口**：`GET /readiness/trip/:tripId/findings/not-applicable`

**功能**：获取标记为"不适用"的检查项列表。

**业务场景**：
- 准备度页面过滤显示
- 用户查看不适用项

**返回数据**：
- `notApplicableItems`: 不适用项列表

**参考实现**：`src/api/readiness.ts` - `getNotApplicableItems()`

---

#### 3.7.8 获取稍后处理项列表

**接口**：`GET /readiness/trip/:tripId/findings/later`

**功能**：获取"稍后处理"的检查项列表。

**业务场景**：
- 准备度页面过滤显示
- 用户查看稍后处理项

**返回数据**：
- `laterItems`: 稍后处理项列表

**参考实现**：`src/api/readiness.ts` - `getLaterItems()`

---

### 3.8 证据刷新接口

#### 3.8.1 刷新证据

**接口**：`POST /readiness/refresh-evidence`

**功能**：刷新检查项的证据（重新获取最新证据）。

**业务场景**：
- 用户点击"刷新证据"按钮
- 证据过期时自动刷新

**请求参数**：
- `tripId`: 行程ID
- `evidenceId`: 证据ID（可选，不提供则刷新所有证据）

**返回数据**：
- `success`: 是否成功

**参考实现**：`src/api/readiness.ts` - `refreshEvidence()`

---

## 📊 四、数据流与状态管理

### 4.1 核心数据流

```
用户进入准备度页面
  ↓
获取行程信息 (tripsApi.getById)
  ↓
并行调用：
  ├─ 获取准备度检查结果 (getTripReadiness)
  ├─ 获取个性化清单 (getPersonalizedChecklist)
  ├─ 获取风险预警 (getRiskWarnings)
  ├─ 获取能力包列表 (getCapabilityPacks)
  └─ 评估能力包 (evaluateCapabilityPacks)
  ↓
展示准备度数据
  ├─ 准备度分数和状态
  ├─ 阻塞项列表
  ├─ 必须项/建议项/可选项
  ├─ 风险预警
  ├─ 能力包评估结果
  └─ 证据链
  ↓
用户交互：
  ├─ 勾选/取消勾选检查项 → updateChecklistStatus
  ├─ 查看修复方案 → getSolutions
  ├─ 应用修复方案 → applyRepair
  ├─ 生成打包清单 → generatePackingList
  └─ 更新打包清单项 → updatePackingListItem
```

### 4.2 状态管理

**前端状态**（`src/pages/readiness/index.tsx`）：
- `readinessData`: 准备度数据（ReadinessData 格式）
- `rawReadinessResult`: 原始检查结果（ReadinessCheckResult 格式）
- `trip`: 行程信息
- `capabilityPacks`: 能力包列表
- `evaluatedPacks`: 能力包评估结果

**后端状态**：
- 准备度检查结果（缓存，有效期根据数据源决定）
- 勾选状态（持久化存储）
- 不适用项标记（持久化存储）
- 稍后处理项（持久化存储）
- 打包清单（持久化存储）

---

## 🎯 五、关键业务规则

### 5.1 准备度状态判断

**状态定义**：
- `ready`: 准备度达标（无阻塞项，必须项完成）
- `nearly`: 接近达标（无阻塞项，但必须项未全部完成）
- `not-ready`: 未达标（存在阻塞项）

**判断规则**：
```typescript
if (totalBlockers > 0) {
  status = 'not-ready';
} else if (totalMust > 0 && mustCompleted < totalMust) {
  status = 'nearly';
} else {
  status = 'ready';
}
```

### 5.2 门控规则

**Should-Exist Gate**：
- **阻塞项存在**：不允许进入执行阶段
- **必须项未完成**：警告，但允许进入执行阶段
- **建议项未完成**：提示，不影响执行

**参考实现**：`src/pages/readiness/index.tsx` - 准备度状态判断逻辑

### 5.3 参数推断规则

**打包清单参数推断**（`src/utils/packing-list-inference.ts`）：
- **季节推断**：基于行程开始日期和目的地
- **路线类型推断**：基于行程天数、地点关键词、路线特征库
- **用户类型推断**：基于预算水平、活动类型、旅行者配置
- **活动类型提取**：从行程项中提取活动关键词

---

## ✅ 六、验收标准

### 6.1 功能验收

- [ ] 能够获取并显示完整的准备度检查结果
- [ ] 能够正确分级显示阻塞项、必须项、建议项、可选项
- [ ] 能够显示风险预警和应对措施
- [ ] 能够显示个性化准备清单
- [ ] 能够显示能力包评估结果
- [ ] 能够生成和管理打包清单
- [ ] 能够勾选/取消勾选检查项
- [ ] 能够查看和应用修复方案
- [ ] 能够标记不适用项和稍后处理项

### 6.2 性能验收

- [ ] 准备度检查接口响应时间 < 3秒
- [ ] 并行接口调用总时间 < 5秒
- [ ] 打包清单生成时间 < 2秒
- [ ] 状态更新响应时间 < 500ms

### 6.3 体验验收

- [ ] 加载状态正确显示
- [ ] 错误处理友好（降级、重试、提示）
- [ ] 空状态友好提示
- [ ] 多语言支持完整
- [ ] 移动端适配良好

---

## 📝 七、待确认问题

1. **缓存策略**：
   - 准备度检查结果缓存时间？
   - 证据刷新频率？

2. **多语言支持**：
   - 哪些接口支持 `lang` 参数？
   - 不支持多语言的接口如何处理？

3. **权限控制**：
   - 准备度数据是否支持分享？
   - 打包清单是否支持协作编辑？

4. **扩展性**：
   - 新增检查项类型的流程？
   - 新增能力包的流程？

---

**文档版本**: v1.0  
**最后更新**: 2026-01-26  
**负责人**: 产品经理（Danny）

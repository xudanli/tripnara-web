# 预算组件样式优化方案

## 📋 问题分析

### 当前问题

1. **颜色使用过于情绪化**
   - 直接使用 `bg-red-50`, `bg-yellow-50`, `bg-green-50` 等饱和色
   - 违反了 TripNARA "颜色承担全部信息"的禁忌
   - 应该更多依赖层级、布局和标签系统

2. **状态系统不一致**
   - 预算状态的颜色映射（红/黄/绿）与裁决状态（ALLOW/NEED_CONFIRM/SUGGEST_REPLACE/REJECT）没有对齐
   - 应该使用更克制的状态表示方式

3. **缺乏设计 Token**
   - 直接使用 Tailwind 的默认颜色，没有使用项目定义的设计 Token
   - 应该使用语义化的颜色 Token

4. **信息层级不够清晰**
   - 预算信息展示缺乏清晰的视觉层级
   - 应该更多依赖排版、间距、字重来区分信息重要性

## 🎨 优化方案

### 1. 定义预算状态设计 Token

在 `tailwind.config.js` 中添加预算状态相关的 Token：

```javascript
colors: {
  // 预算状态颜色（克制、中性）
  'budget-safe': 'var(--budget-safe)',           // 预算充足（中性绿）
  'budget-safe-foreground': 'var(--budget-safe-foreground)',
  'budget-safe-border': 'var(--budget-safe-border)',
  'budget-warning': 'var(--budget-warning)',     // 预算警告（中性黄）
  'budget-warning-foreground': 'var(--budget-warning-foreground)',
  'budget-warning-border': 'var(--budget-warning-border)',
  'budget-critical': 'var(--budget-critical)',   // 预算超支（中性红）
  'budget-critical-foreground': 'var(--budget-critical-foreground)',
  'budget-critical-border': 'var(--budget-critical-border)',
}
```

### 2. 在 globals.css 中定义 CSS 变量

```css
:root {
  /* 预算状态颜色 - 克制、中性 */
  --budget-safe: oklch(0.95 0.02 150);           /* 浅绿，低饱和度 */
  --budget-safe-foreground: oklch(0.35 0.05 150);
  --budget-safe-border: oklch(0.85 0.03 150);
  
  --budget-warning: oklch(0.97 0.02 80);          /* 浅黄，低饱和度 */
  --budget-warning-foreground: oklch(0.40 0.05 80);
  --budget-warning-border: oklch(0.88 0.03 80);
  
  --budget-critical: oklch(0.96 0.02 20);        /* 浅红，低饱和度 */
  --budget-critical-foreground: oklch(0.40 0.05 20);
  --budget-critical-border: oklch(0.85 0.03 20);
}
```

### 3. 优化组件样式原则

#### 原则 1: 信息层级优先于颜色
- 使用字重、字号、间距来区分信息重要性
- 颜色仅作为辅助标识，不承担主要信息传递

#### 原则 2: 状态表示克制
- 使用边框、图标、标签来标识状态
- 背景色使用极浅的色调（oklch 0.95+）
- 文字颜色使用中性色，仅在必要时使用状态色

#### 原则 3: 一致性
- 预算状态与裁决状态使用相似的设计语言
- 保持组件间的视觉一致性

## 🔧 具体优化

### BudgetOverviewCard 优化

**当前问题**:
- 使用 `bg-red-50`, `text-red-600` 等情绪化颜色
- 进度条背景色过于饱和

**优化方案**:
- 使用 `budget-safe/warning/critical` Token
- 进度条使用更克制的颜色
- 增强信息层级（字重、间距）

### BudgetAlertBanner 优化

**当前问题**:
- Alert 组件使用饱和的背景色和边框色

**优化方案**:
- 使用 `budget-warning/critical` Token
- 减少背景色的视觉重量
- 更多依赖图标和文字来传达信息

### BudgetMonitorCard 优化

**当前问题**:
- 与 BudgetOverviewCard 类似的问题

**优化方案**:
- 统一使用预算状态 Token
- 优化实时监控的视觉呈现

### DayItineraryCard 预算概览优化

**当前问题**:
- 预算信息使用红/绿对比过于强烈

**优化方案**:
- 使用更中性的颜色表示
- 优化信息密度和可读性

## 📐 实施步骤

1. ✅ 添加预算状态 Token 到 tailwind.config.js
2. ✅ 定义 CSS 变量到 globals.css
3. ✅ 优化 BudgetOverviewCard 组件
4. ✅ 优化 BudgetAlertBanner 组件
5. ✅ 优化 BudgetMonitorCard 组件
6. ✅ 优化 DayItineraryCard 预算部分
7. ✅ 优化 QuickExpenseEditor 组件（如需要）

## ✅ 验收标准

- [ ] 颜色使用克制，不情绪化
- [ ] 信息层级清晰，依赖排版而非颜色
- [ ] 状态系统一致，与裁决状态对齐
- [ ] 使用设计 Token，而非硬编码颜色
- [ ] 符合 TripNARA 设计哲学（Clarity over Charm）

# 准备度抽屉重构 PRD

## 0.1 项目背景与问题定义（Why Now）

### 现状问题

**准备度抽屉（ReadinessDrawer）** 是用户查看行程准备度检查结果的核心入口，当前存在以下问题：

1. **信息冗余严重**
   - 顶部汇总区包含过多信息：分数、状态标签、4个统计指标、5个证据状态指标、3个数据新鲜度时间戳
   - 总计超过 **12个数据点** 在顶部固定区域，用户难以快速聚焦核心问题
   - 证据状态摘要和数据新鲜度信息重复，且占用大量垂直空间

2. **功能重复**
   - **冰岛信息源显示** 在 `ReadinessDrawer` 和 `PlanningWorkbenchTab` 中都有实现
   - 两个位置功能完全一致，造成维护成本和用户困惑
   - 冰岛信息源属于"规划阶段"信息，不应出现在"准备度检查"抽屉中

3. **操作按钮过多**
   - 顶部有3个主要操作按钮：刷新、获取证据、生成打包清单
   - 按钮布局分散，用户需要多次点击才能完成核心操作
   - "获取证据"按钮功能与自动获取证据（`useAutoFetchEvidence`）重复

4. **组件代码臃肿**
   - 当前组件代码量：**1151行**
   - 包含过多状态管理和业务逻辑，难以维护
   - 调试信息占用底部空间，但默认隐藏，价值有限

### 用户痛点

- **信息过载**：打开抽屉后看到太多数据，不知道先看什么
- **操作混乱**：不知道应该先刷新、获取证据还是生成打包清单
- **功能重复**：在规划工作台和准备度抽屉都看到冰岛信息，不知道区别
- **加载缓慢**：顶部固定区域内容过多，影响首次渲染性能

### 为什么现在重构

1. **用户体验瓶颈**：准备度检查是核心功能，但当前体验阻碍了用户快速理解问题
2. **维护成本高**：代码臃肿，新增功能困难
3. **功能定位不清**：准备度抽屉应该聚焦"检查结果"，而不是"规划信息"
4. **性能优化机会**：简化UI可以提升渲染性能

---

## 0.2 目标与成功指标（North Star & Metrics）

### 核心目标

**简化准备度抽屉，聚焦核心价值：让用户快速理解行程准备度问题和解决方案**

### 北极星指标

- **准备度问题解决率**：用户在准备度抽屉中完成操作（勾选、标记不适用、添加到稍后）的比例
- **准备度检查完成时间**：从打开抽屉到用户理解所有问题并完成操作的平均时间

### 过程指标

- **信息密度**：顶部固定区域的数据点数量（目标：从12个减少到≤6个）
- **操作步骤**：完成核心操作的平均点击次数（目标：减少30%）
- **代码行数**：组件代码量（目标：从1151行减少到≤800行）
- **首次渲染时间**：抽屉打开到内容显示的时间（目标：减少20%）

### 质量指标

- **用户满意度**：准备度抽屉的NPS评分
- **错误率**：用户操作失败的比例
- **功能使用率**：各功能按钮的点击率

---

## 0.3 用户与场景（Persona / JTBD / User Journey）

### 目标用户

1. **新手用户**（首次使用准备度检查）
   - 需要：快速理解"我有什么问题需要解决"
   - 痛点：信息太多，不知道从哪里开始

2. **熟练用户**（定期检查准备度）
   - 需要：快速查看新增问题和更新状态
   - 痛点：重复信息太多，影响效率

3. **极端情况用户**（复杂行程，问题很多）
   - 需要：按优先级处理问题，避免遗漏
   - 痛点：信息混乱，难以聚焦关键问题

### 用户旅程

```
用户打开行程详情页
    ↓
点击"准备度检查"按钮
    ↓
【重构前】看到12+个数据点，信息过载
【重构后】看到核心状态和关键问题，快速理解
    ↓
【重构前】不知道先做什么，尝试多个按钮
【重构后】按优先级处理问题（阻塞项 → 必须项 → 建议项）
    ↓
完成准备度检查
    ↓
【重构前】关闭抽屉，但可能遗漏重要信息
【重构后】明确知道已完成所有关键检查
```

---

## 0.4 需求范围（In/Out）与约束

### ✅ 重构范围（In）

1. **简化顶部汇总区**
   - 保留：核心状态（BLOCK/WARN/PASS）、总体分数
   - 简化：统计信息（4个指标 → 2个关键指标）
   - 移除：证据状态摘要（5个指标）、数据新鲜度（3个时间戳）

2. **移除冰岛信息源显示**
   - 从 `ReadinessDrawer` 中完全移除冰岛信息源相关代码
   - 保留在 `PlanningWorkbenchTab` 中（规划阶段更合适）

3. **优化操作按钮**
   - 合并：将"刷新"和"获取证据"合并为一个"刷新"按钮（自动获取证据）
   - 保留：生成打包清单按钮（独立功能）
   - 简化：按钮布局从2行减少到1行

4. **组件拆分**
   - 将 `ReadinessDrawer` 拆分为多个子组件：
     - `ReadinessDrawerHeader`（顶部汇总）
     - `ReadinessDrawerContent`（核心内容）
     - `ReadinessDrawerActions`（操作按钮）

### ❌ 不在本次重构范围（Out）

1. **不改变核心功能**
   - 准备度检查逻辑不变
   - API 接口不变
   - 数据模型不变

2. **不改变核心内容区**
   - Findings 分组显示逻辑不变
   - ChecklistSection 组件不变
   - RiskCard 组件不变

3. **不改变调试功能**
   - 调试信息区域保留（但可以优化位置）

### 约束条件

- **向后兼容**：API 接口不变，不影响其他页面
- **性能要求**：首次渲染时间不能增加
- **可访问性**：保持键盘导航和屏幕阅读器支持
- **国际化**：保持多语言支持

---

## 0.5 竞品与对标

### 竞品分析

**假设**：参考其他旅行规划应用的准备度检查功能（待确认具体竞品）

**常见模式**：
1. **简化顶部**：只显示核心状态和分数
2. **分层信息**：关键问题优先，详细信息可展开
3. **操作集中**：主要操作按钮集中在顶部或底部

### 设计原则

- **信息层次**：最重要信息最显眼，次要信息可折叠
- **操作简化**：减少操作步骤，合并相似功能
- **功能聚焦**：每个页面/组件只做一件事

---

## 0.6 总体方案概览

### 重构前后对比

#### 重构前（当前状态）

```
┌─────────────────────────────────────┐
│ 顶部汇总区（固定，12+个数据点）      │
│ - 分数 + 状态标签                   │
│ - 4个统计指标                       │
│ - 5个证据状态指标                   │
│ - 3个数据新鲜度时间戳               │
│ - 3个操作按钮（刷新、获取证据、打包）│
│ - 冰岛信息源（仅冰岛行程）          │
├─────────────────────────────────────┤
│ 核心内容区（滚动）                  │
│ - Findings 分组显示                 │
│ - 风险概览                          │
├─────────────────────────────────────┤
│ 调试区（默认隐藏）                  │
└─────────────────────────────────────┘
```

#### 重构后（目标状态）

```
┌─────────────────────────────────────┐
│ 顶部汇总区（固定，≤6个数据点）      │
│ - 分数 + 状态标签                   │
│ - 2个关键统计指标（阻塞项、警告项） │
│ - 1个操作按钮（刷新，自动获取证据） │
│ - 1个操作按钮（生成打包清单）       │
├─────────────────────────────────────┤
│ 核心内容区（滚动）                  │
│ - Findings 分组显示                 │
│ - 风险概览                          │
├─────────────────────────────────────┤
│ 调试区（默认隐藏，移至右上角菜单）  │
└─────────────────────────────────────┘
```

### 核心改动

1. **顶部汇总区简化**
   - 移除证据状态摘要（5个指标）
   - 移除数据新鲜度（3个时间戳）
   - 统计指标从4个减少到2个（只保留阻塞项和警告项）
   - 操作按钮从3个减少到2个（合并刷新和获取证据）

2. **移除冰岛信息源**
   - 完全移除冰岛信息源相关代码（约90行）
   - 移除相关 Hook 调用和状态管理

3. **组件拆分**
   - 创建 `ReadinessDrawerHeader` 组件
   - 创建 `ReadinessDrawerActions` 组件
   - 主组件 `ReadinessDrawer` 只负责数据加载和布局

### 技术方案

- **不涉及后端 API 变更**：所有改动都在前端
- **不涉及数据模型变更**：使用现有数据结构
- **组件拆分**：使用 React 组件组合模式
- **状态管理**：保持现有 React Hooks 模式

---

## 0.7 关键流程（用户流 + 系统流 + 异常流）

### 用户流程

#### 主要流程：查看准备度检查结果

```
1. 用户打开行程详情页
   ↓
2. 点击"准备度检查"按钮
   ↓
3. 【重构后】抽屉打开，显示：
   - 核心状态（BLOCK/WARN/PASS）
   - 总体分数
   - 2个关键统计指标（阻塞项、警告项）
   ↓
4. 用户查看核心内容区：
   - 按优先级查看 Findings（阻塞项 → 必须项 → 建议项）
   - 查看风险概览
   ↓
5. 用户执行操作：
   - 勾选已完成项
   - 标记不适用项
   - 添加到稍后处理
   ↓
6. 用户点击"刷新"按钮（自动获取证据）
   ↓
7. 用户点击"生成打包清单"（如需要）
   ↓
8. 关闭抽屉
```

#### 次要流程：刷新数据

```
1. 用户点击"刷新"按钮
   ↓
2. 【重构后】系统自动执行：
   - 刷新准备度检查结果
   - 自动获取证据数据（静默模式）
   - 更新顶部汇总区
   ↓
3. 显示刷新成功提示
```

### 系统流程

#### 数据加载流程

```
1. 抽屉打开（open = true）
   ↓
2. 并行加载数据：
   - tripsApi.getById(tripId)
   - readinessApi.getTripReadiness(tripId)
   - readinessApi.getScoreBreakdown(tripId)
   - readinessApi.getCoverageMapData(tripId)
   ↓
3. 【重构后】不再加载冰岛信息源数据
   ↓
4. 更新状态：
   - setTrip(tripData)
   - setReadinessResult(readinessData)
   - setScoreBreakdown(scoreData)
   - setCoverageMapData(coverageData)
   ↓
5. 自动获取证据（useAutoFetchEvidence，静默模式）
   ↓
6. 渲染UI
```

#### 刷新流程

```
1. 用户点击"刷新"按钮
   ↓
2. 设置 refreshing = true
   ↓
3. 并行执行：
   - loadData()（重新加载所有数据）
   - loadCheckedStatus()（重新加载勾选状态）
   - loadMarkedItems()（重新加载标记项）
   ↓
4. 【重构后】自动触发证据获取（useAutoFetchEvidence）
   ↓
5. 设置 refreshing = false
   ↓
6. 显示成功提示
```

### 异常流程

#### 数据加载失败

```
1. API 调用失败
   ↓
2. 捕获错误，记录日志
   ↓
3. 【重构后】显示简化错误提示（不显示详细错误信息）
   ↓
4. 保持抽屉打开，显示空状态
```

#### 网络超时

```
1. 请求超时
   ↓
2. 显示超时提示
   ↓
3. 提供重试按钮
```

---

## 0.8 页面与交互设计

### 信息架构

#### 重构前

```
ReadinessDrawer
├── 顶部汇总区（固定）
│   ├── 分数 + 状态标签
│   ├── 统计信息（4个指标）
│   ├── 证据状态摘要（5个指标）
│   ├── 数据新鲜度（3个时间戳）
│   ├── 操作按钮（3个）
│   └── 冰岛信息源（条件显示）
├── 核心内容区（滚动）
│   ├── Findings 分组显示
│   └── 风险概览
└── 调试区（默认隐藏）
```

#### 重构后

```
ReadinessDrawer
├── ReadinessDrawerHeader（顶部汇总）
│   ├── 分数 + 状态标签
│   ├── 关键统计（2个指标）
│   └── ReadinessDrawerActions（操作按钮）
├── ReadinessDrawerContent（核心内容）
│   ├── Findings 分组显示
│   └── 风险概览
└── 调试菜单（右上角，默认隐藏）
```

### 组件设计

#### ReadinessDrawerHeader

**职责**：显示核心状态和关键统计

**Props**:
```typescript
interface ReadinessDrawerHeaderProps {
  scoreBreakdown: ScoreBreakdownResponse | null;
  gateStatus: GateStatus;
  readinessResult: ReadinessCheckResult | null;
}
```

**显示内容**:
- 分数圆圈（16x16，带颜色边框）
- 状态标签（BLOCK/WARN/PASS）
- 2个关键统计指标：
  - 阻塞项数量（红色）
  - 警告项数量（黄色）

**移除内容**:
- ❌ 建议项数量
- ❌ 风险数量
- ❌ 证据状态摘要（5个指标）
- ❌ 数据新鲜度（3个时间戳）

#### ReadinessDrawerActions

**职责**：提供操作按钮

**Props**:
```typescript
interface ReadinessDrawerActionsProps {
  onRefresh: () => void;
  onGeneratePackingList: () => void;
  refreshing: boolean;
  generatingPackingList: boolean;
}
```

**按钮布局**:
- 1行2个按钮：
  - "刷新"按钮（flex-1）：合并刷新和获取证据功能
  - "生成打包清单"按钮（flex-1）

**移除内容**:
- ❌ 独立的"获取证据"按钮（功能合并到刷新）

#### ReadinessDrawerContent

**职责**：显示核心内容（Findings 和风险）

**Props**:
```typescript
interface ReadinessDrawerContentProps {
  readinessResult: ReadinessCheckResult | null;
  scoreBreakdown: ScoreBreakdownResponse | null;
  trip: TripDetail | null;
  // ... 其他 props
}
```

**显示内容**:
- Findings 分组显示（按 category 分组）
- 风险概览

**保持不变**:
- ✅ Findings 显示逻辑
- ✅ ChecklistSection 组件
- ✅ RiskCard 组件

### 交互设计

#### 顶部汇总区

**布局**:
```
┌─────────────────────────────────────┐
│ [分数圆圈] [状态标签]               │
│                                     │
│ [阻塞项: X] [警告项: Y]            │
│                                     │
│ [刷新] [生成打包清单]               │
└─────────────────────────────────────┘
```

**响应式**:
- 宽度固定：480px（抽屉宽度）
- 高度自适应：根据内容调整

#### 操作按钮

**刷新按钮**:
- 点击后自动执行：
  1. 刷新准备度检查结果
  2. 自动获取证据数据（静默模式）
  3. 更新所有状态
- 显示加载状态：按钮显示 Spinner

**生成打包清单按钮**:
- 点击后跳转到打包清单页面
- 显示加载状态：按钮显示 Spinner

### 状态设计

#### 加载状态

```typescript
type LoadingState = {
  loading: boolean;        // 初始加载
  refreshing: boolean;     // 刷新中
  generatingPackingList: boolean; // 生成打包清单中
};
```

#### 数据状态

```typescript
type DataState = {
  trip: TripDetail | null;
  readinessResult: ReadinessCheckResult | null;
  scoreBreakdown: ScoreBreakdownResponse | null;
  coverageMapData: CoverageMapResponse | null;
};
```

**移除状态**:
- ❌ `icelandInfo` 相关状态
- ❌ `isIceland` 状态
- ❌ `fetchingEvidence` 状态（合并到 `refreshing`）

---

## 0.11.1 视觉设计规范

### GateStatus 视觉规范

**原则**：**四态裁决必须系统级一致**，符合 TripNARA 的"决策是 UI 原语"原则。

**视觉策略**：
- **避免情绪化大色块**：不使用 `bg-red-500`、`bg-yellow-500`、`bg-green-500` 这种高饱和度纯色
- **使用层级、描边、icon、标签**：通过视觉层级传达状态，而不是颜色

**GateStatus Token**：

```typescript
const gateStatusTokens = {
  BLOCK: {
    border: 'border-red-600',
    text: 'text-red-700',
    bg: 'bg-red-50',
    icon: AlertCircle,
    iconColor: 'text-red-600',
  },
  WARN: {
    border: 'border-amber-600',
    text: 'text-amber-700',
    bg: 'bg-amber-50',
    icon: AlertTriangle,
    iconColor: 'text-amber-600',
  },
  PASS: {
    border: 'border-green-600',
    text: 'text-green-700',
    bg: 'bg-green-50',
    icon: CheckCircle2,
    iconColor: 'text-green-600',
  },
};
```

**组件实现**：

```tsx
<Badge
  variant="outline"
  className={cn(
    'w-full justify-center py-2 text-sm font-semibold',
    gateStatusTokens[gateStatus].border,
    gateStatusTokens[gateStatus].bg,
    gateStatusTokens[gateStatus].text
  )}
>
  <Icon className={cn('mr-2 h-4 w-4', gateStatusTokens[gateStatus].iconColor)} />
  {label}
</Badge>
```

**理由**：
- ✅ 符合 TripNARA 的"克制"原则（Quiet confidence）
- ✅ 避免情绪化，保持专业感
- ✅ 通过层级和描边传达状态，而不是纯色块

---

### 分数圆圈视觉规范

**尺寸规范**：
- 圆圈尺寸：`w-16 h-16`（64x64px）
- 边框宽度：`border-2`（8px，不使用 `border-4`）
- 数字字体：`text-xl font-bold`（20px，粗体）
- 分母字体：`text-xs text-gray-500`（12px，灰色）

**颜色规范**：
- `< 60`：`border-red-600` + `bg-red-50` + `text-red-700`
- `60-79`：`border-amber-600` + `bg-amber-50` + `text-amber-700`
- `≥ 80`：`border-green-600` + `bg-green-50` + `text-green-700`

**组件实现**：

```tsx
<div className={cn(
  'flex-shrink-0 w-16 h-16 rounded-full flex flex-col items-center justify-center border-2',
  scoreBreakdown.score.overall < 60 
    ? 'border-red-600 bg-red-50 text-red-700'
    : scoreBreakdown.score.overall < 80
    ? 'border-amber-600 bg-amber-50 text-amber-700'
    : 'border-green-600 bg-green-50 text-green-700'
)}>
  <span className="text-xl font-bold">{scoreBreakdown.score.overall}</span>
  <span className="text-xs text-gray-500">/100</span>
</div>
```

---

### 统计指标视觉规范

**布局规范**：
- 网格布局：`grid grid-cols-2 gap-3`（2列，间距12px）
- 对齐方式：`text-center`（居中）
- 内边距：`px-4 pb-3`（水平16px，底部12px）

**Typography 规范**：
- 数字：`text-base font-semibold`（16px，半粗体）
- 标签：`text-xs text-gray-600 mt-0.5`（12px，灰色，顶部间距2px）

**颜色规范**：
- 阻塞项：`text-red-700`
- 警告项：`text-amber-700`

**组件实现**：

```tsx
<div className="grid grid-cols-2 gap-3 px-4 pb-3">
  <div className="text-center">
    <div className="text-base font-semibold text-red-700">
      {scoreBreakdown.summary.blockers || 0}
    </div>
    <div className="text-xs text-gray-600 mt-0.5">
      {t('dashboard.readiness.page.drawer.stats.blockers')}
    </div>
  </div>
  <div className="text-center">
    <div className="text-base font-semibold text-amber-700">
      {scoreBreakdown.summary.warnings || 0}
    </div>
    <div className="text-xs text-gray-600 mt-0.5">
      {t('dashboard.readiness.page.drawer.stats.must')}
    </div>
  </div>
</div>
```

---

### 操作按钮视觉规范

**布局规范**：
- 容器：`flex items-center gap-2 px-4 pb-4`（水平布局，间距8px，内边距16px）
- 按钮宽度：`flex-1`（等宽）

**按钮样式规范**：
- 变体：`variant="outline"`（轮廓样式，符合 TripNARA 克制风格）
- 尺寸：`size="sm"`（小尺寸，不抢夺主要内容）
- 图标：`h-4 w-4`（16x16px，使用 `lucide-react`）

**加载状态规范**：
- 加载时显示 `Spinner` 组件
- 加载时禁用按钮（`disabled={loading}`）
- 加载文案：`刷新中...`、`生成中...`

**组件实现**：

```tsx
<div className="flex items-center gap-2 px-4 pb-4">
  <Button
    variant="outline"
    size="sm"
    onClick={onRefresh}
    disabled={refreshing}
    className="flex-1"
  >
    {refreshing ? (
      <>
        <Spinner className="mr-2 h-4 w-4" />
        <span>刷新中...</span>
      </>
    ) : (
      <>
        <RefreshCw className="mr-2 h-4 w-4" />
        <span>刷新</span>
      </>
    )}
  </Button>
  <Button
    variant="outline"
    size="sm"
    onClick={onGeneratePackingList}
    disabled={generatingPackingList}
    className="flex-1"
  >
    {generatingPackingList ? (
      <>
        <Spinner className="mr-2 h-4 w-4" />
        <span>生成中...</span>
      </>
    ) : (
      <>
        <Package className="mr-2 h-4 w-4" />
        <span>生成打包清单</span>
      </>
    )}
  </Button>
</div>
```

---

### 信息层级和间距规范

**层级1：核心状态（最高优先级）**
- 内容：分数圆圈 + 状态标签
- 间距：`pt-4 pb-3`（顶部16px，底部12px）

**层级2：关键统计（次要优先级）**
- 内容：2个统计指标
- 间距：`pb-3`（底部12px）

**层级3：操作按钮（最低优先级）**
- 内容：2个操作按钮
- 间距：`pb-4`（底部16px）

**间距 Token**：
- `px-4` = 16px（水平内边距）
- `pt-4` = 16px（顶部内边距）
- `pb-3` = 12px（中间内边距）
- `pb-4` = 16px（底部内边距）
- `gap-3` = 12px（元素间距）
- `gap-2` = 8px（按钮间距）

---

## 0.11.2 设计 Token 定义

### GateStatus 颜色 Token

```typescript
// tailwind.config.js 或 CSS Variables
const gateStatusColors = {
  block: {
    border: 'border-red-600',    // #dc2626
    text: 'text-red-700',         // #b91c1c
    bg: 'bg-red-50',              // #fef2f2
    icon: 'text-red-600',         // #dc2626
  },
  warn: {
    border: 'border-amber-600',   // #d97706
    text: 'text-amber-700',       // #b45309
    bg: 'bg-amber-50',            // #fffbeb
    icon: 'text-amber-600',      // #d97706
  },
  pass: {
    border: 'border-green-600',   // #16a34a
    text: 'text-green-700',       // #15803d
    bg: 'bg-green-50',            // #f0fdf4
    icon: 'text-green-600',       // #16a34a
  },
};
```

### 分数圆圈颜色 Token

```typescript
const scoreColors = {
  low: {
    border: 'border-red-600',     // #dc2626
    bg: 'bg-red-50',              // #fef2f2
    text: 'text-red-700',         // #b91c1c
  },
  medium: {
    border: 'border-amber-600',   // #d97706
    bg: 'bg-amber-50',            // #fffbeb
    text: 'text-amber-700',       // #b45309
  },
  high: {
    border: 'border-green-600',   // #16a34a
    bg: 'bg-green-50',            // #f0fdf4
    text: 'text-green-700',       // #15803d
  },
};
```

### 间距 Token

```typescript
const spacingTokens = {
  drawerPadding: 'px-4',              // 16px（水平内边距）
  drawerPaddingTop: 'pt-4',           // 16px（顶部内边距）
  drawerPaddingBottom: 'pb-4',         // 16px（底部内边距）
  drawerPaddingBottomSmall: 'pb-3',    // 12px（中间内边距）
  drawerGap: 'gap-3',                 // 12px（元素间距）
  drawerGapSmall: 'gap-2',            // 8px（按钮间距）
};
```

### Typography Token

```typescript
const typographyTokens = {
  scoreNumber: 'text-xl font-bold',        // 20px，粗体（分数数字）
  scoreDenominator: 'text-xs text-gray-500', // 12px，灰色（分数分母）
  statNumber: 'text-base font-semibold',   // 16px，半粗体（统计数字）
  statLabel: 'text-xs text-gray-600',      // 12px，灰色（统计标签）
  statusLabel: 'text-sm font-semibold',     // 14px，半粗体（状态标签）
  buttonText: 'text-sm',                   // 14px（按钮文字）
};
```

### Tailwind Config 补充

```javascript
// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      colors: {
        gate: {
          block: {
            border: '#dc2626',
            text: '#b91c1c',
            bg: '#fef2f2',
            icon: '#dc2626',
          },
          warn: {
            border: '#d97706',
            text: '#b45309',
            bg: '#fffbeb',
            icon: '#d97706',
          },
          pass: {
            border: '#16a34a',
            text: '#15803d',
            bg: '#f0fdf4',
            icon: '#16a34a',
          },
        },
        score: {
          low: {
            border: '#dc2626',
            bg: '#fef2f2',
            text: '#b91c1c',
          },
          medium: {
            border: '#d97706',
            bg: '#fffbeb',
            text: '#b45309',
          },
          high: {
            border: '#16a34a',
            bg: '#f0fdf4',
            text: '#15803d',
          },
        },
      },
      spacing: {
        'drawer-padding': '16px',
        'drawer-padding-small': '12px',
        'drawer-gap': '12px',
        'drawer-gap-small': '8px',
      },
    },
  },
};
```

---

## 0.11.3 状态系统视觉规范

### 加载状态视觉反馈

#### 初始加载状态

**视觉表现**：
- 显示 `Spinner` 组件（居中）
- 尺寸：`w-6 h-6`（24x24px）
- 颜色：使用主题色（`text-gray-600`）

**组件实现**：

```tsx
{loading ? (
  <div className="flex items-center justify-center py-12">
    <Spinner className="w-6 h-6" />
  </div>
) : (
  // 内容
)}
```

#### 刷新状态视觉反馈

**视觉表现**：
- 按钮显示 `Spinner` 图标（`h-4 w-4`）
- 按钮文字变为"刷新中..."
- 按钮禁用（`disabled={refreshing}`）

**组件实现**：

```tsx
<Button
  variant="outline"
  size="sm"
  onClick={handleRefresh}
  disabled={refreshing}
  className="flex-1"
>
  {refreshing ? (
    <>
      <Spinner className="mr-2 h-4 w-4" />
      <span>刷新中...</span>
    </>
  ) : (
    <>
      <RefreshCw className="mr-2 h-4 w-4" />
      <span>刷新</span>
    </>
  )}
</Button>
```

#### 生成打包清单状态视觉反馈

**视觉表现**：
- 按钮显示 `Spinner` 图标（`h-4 w-4`）
- 按钮文字变为"生成中..."
- 按钮禁用（`disabled={generatingPackingList}`）

**组件实现**：

```tsx
<Button
  variant="outline"
  size="sm"
  onClick={handleGeneratePackingList}
  disabled={generatingPackingList}
  className="flex-1"
>
  {generatingPackingList ? (
    <>
      <Spinner className="mr-2 h-4 w-4" />
      <span>生成中...</span>
    </>
  ) : (
    <>
      <Package className="mr-2 h-4 w-4" />
      <span>生成打包清单</span>
    </>
  )}
</Button>
```

---

### 错误状态视觉反馈

**视觉表现**：
- 显示错误提示（使用 `toast.error`）
- 错误信息清晰、可操作
- 不阻塞用户操作（允许重试）

**组件实现**：

```tsx
try {
  await loadData();
} catch (err) {
  console.error('Failed to load readiness data:', err);
  toast.error(t('dashboard.readiness.page.drawer.errors.loadFailed'));
  // 保持抽屉打开，显示空状态
}
```

---

### 空状态视觉反馈

**视觉表现**：
- 显示友好的空状态提示
- 文字居中，使用 `text-gray-500`
- 提供操作建议（如"点击刷新"）

**组件实现**：

```tsx
{!readinessResult ? (
  <div className="text-center py-12 text-gray-500 text-sm">
    {t('dashboard.readiness.page.drawer.noData')}
  </div>
) : (
  // 内容
)}
```

---

## 0.11.4 动效规范

### 抽屉打开/关闭动效

**动效参数**：
- 持续时间：`duration-300`（300ms）
- 缓动函数：`ease-in-out`（默认）
- 变换属性：`translate-x`（水平移动）

**组件实现**：

```tsx
<div
  className={cn(
    'fixed right-0 top-0 h-full w-[480px] bg-white border-l border-gray-200 shadow-xl z-50 transition-transform duration-300',
    open ? 'translate-x-0' : 'translate-x-full'
  )}
>
  {/* 内容 */}
</div>
```

**动效语义**：
- **入场**：从右侧滑入（`translate-x-full` → `translate-x-0`）
- **退场**：向右侧滑出（`translate-x-0` → `translate-x-full`）

---

### 加载状态动效

**Spinner 旋转动效**：

**动效参数**：
- 持续时间：`animate-spin`（1s 循环）
- 缓动函数：`linear`（匀速）

**组件实现**：

```tsx
<Spinner className="w-6 h-6 animate-spin" />
```

**动效语义**：
- **加载中**：无限旋转，表示系统正在处理
- **完成**：停止旋转，显示结果

---

### 状态切换动效

**GateStatus 切换动效**：

**动效参数**：
- 持续时间：`transition-colors duration-200`（200ms）
- 缓动函数：`ease-in-out`（默认）

**组件实现**：

```tsx
<Badge
  variant="outline"
  className={cn(
    'w-full justify-center py-2 text-sm font-semibold transition-colors duration-200',
    gateStatusTokens[gateStatus].border,
    gateStatusTokens[gateStatus].bg,
    gateStatusTokens[gateStatus].text
  )}
>
  {/* 内容 */}
</Badge>
```

**动效语义**：
- **状态切换**：颜色平滑过渡，避免突兀变化
- **视觉反馈**：用户能感知到状态变化

---

### 按钮交互动效

**按钮点击动效**：

**动效参数**：
- 持续时间：`transition-all duration-150`（150ms）
- 缓动函数：`ease-in-out`（默认）

**组件实现**：

```tsx
<Button
  variant="outline"
  size="sm"
  className="flex-1 transition-all duration-150 hover:scale-[1.02] active:scale-[0.98]"
>
  {/* 内容 */}
</Button>
```

**动效语义**：
- **悬停**：轻微放大（`scale-[1.02]`），提供视觉反馈
- **点击**：轻微缩小（`scale-[0.98]`），模拟按压效果

---

### 动效禁用场景

**低性能设备**：
- 使用 `prefers-reduced-motion` 媒体查询
- 禁用非必要的动效

**组件实现**：

```tsx
<div
  className={cn(
    'transition-transform duration-300',
    'motion-reduce:transition-none' // 低性能设备禁用动效
  )}
>
  {/* 内容 */}
</div>
```

**省电模式**：
- 减少动效持续时间
- 使用更简单的动效（如透明度变化）

---

## 0.11.5 组件视觉一致性规范

### 组件间视觉一致性

**原则**：**One system, many surfaces（一个系统，多端一致）**

**Token 统一**：
- 所有组件使用相同的间距 Token（`px-4`、`pb-3`、`pb-4`）
- 所有组件使用相同的颜色 Token（`text-gray-600`、`border-gray-200`）
- 所有组件使用相同的 Typography Token（`text-xs`、`text-sm`、`font-semibold`）

**组件规范**：

```typescript
// ReadinessDrawerHeader 组件规范
interface ReadinessDrawerHeaderProps {
  scoreBreakdown: ScoreBreakdownResponse | null;
  gateStatus: GateStatus;
  readinessResult: ReadinessCheckResult | null;
}

// 视觉规范：
// - 容器：flex-shrink-0 border-b border-gray-200 bg-white
// - 内边距：px-4（水平），pt-4 pb-3 pb-4（垂直，分层）
// - 间距：gap-3（元素间距）
```

```typescript
// ReadinessDrawerActions 组件规范
interface ReadinessDrawerActionsProps {
  onRefresh: () => void;
  onGeneratePackingList: () => void;
  refreshing: boolean;
  generatingPackingList: boolean;
}

// 视觉规范：
// - 容器：flex items-center gap-2 px-4 pb-4
// - 按钮：variant="outline" size="sm" flex-1
// - 图标：h-4 w-4（lucide-react）
```

---

## 0.9 数据模型与字段字典

### 核心数据结构

#### ScoreBreakdownResponse（保持不变）

```typescript
interface ScoreBreakdownResponse {
  score: {
    overall: number;      // 总体分数（保留）
    // ... 其他分数
  };
  summary: {
    blockers: number;     // 阻塞项数量（保留）
    warnings: number;     // 警告项数量（保留）
    suggestions: number;  // 建议项数量（移除显示）
    // ... 其他统计
  };
  findings: Finding[];
}
```

**使用变更**:
- ✅ 保留：`score.overall`（显示分数）
- ✅ 保留：`summary.blockers`（显示阻塞项数量）
- ✅ 保留：`summary.warnings`（显示警告项数量）
- ❌ 移除显示：`summary.suggestions`（不再在顶部显示）
- ❌ 移除显示：`readinessResult.risks.length`（不再在顶部显示）

#### CoverageMapResponse（部分移除显示）

```typescript
interface CoverageMapResponse {
  evidenceStatusSummary: {
    total: number;        // 移除显示
    fetched: number;      // 移除显示
    fetching: number;      // 移除显示
    missing: number;      // 移除显示
    failed: number;       // 移除显示
  };
  dataFreshness: {
    weather: string;      // 移除显示
    roadClosure: string; // 移除显示
    openingHours: string; // 移除显示
  };
  // ... 其他字段（保留，用于核心内容区）
}
```

**使用变更**:
- ❌ 移除显示：`evidenceStatusSummary`（5个指标）
- ❌ 移除显示：`dataFreshness`（3个时间戳）
- ✅ 保留：`coverageMapData` 对象本身（用于核心内容区的证据显示）

### 移除的数据结构

#### IcelandInfo 相关（完全移除）

```typescript
// 移除以下 Hook 调用和状态：
const isIceland = useIsIcelandTrip(trip?.destination);
const icelandInfoParams = inferIcelandInfoParams(trip);
const icelandInfo = useIcelandInfo({ ... });
```

**影响**:
- 移除约 **90行代码**
- 移除相关 import：
  - `useIcelandInfo`
  - `useIsIcelandTrip`
  - `inferIcelandInfoParams`

---

## 0.10 服务端与接口（API、权限、缓存、降级、容灾）

### API 接口（无变更）

**保持不变**:
- ✅ `GET /api/trips/:id` - 获取行程详情
- ✅ `GET /api/readiness/trip/:id` - 获取准备度检查结果
- ✅ `GET /api/readiness/trip/:id/score-breakdown` - 获取分数分解
- ✅ `GET /api/readiness/trip/:id/coverage-map` - 获取覆盖地图数据
- ✅ `POST /api/planning-workbench/trips/:tripId/fetch-evidence` - 获取证据（自动调用）

**移除调用**:
- ❌ `icelandInfoApi.getWeather()` - 移除
- ❌ `icelandInfoApi.getSafety()` - 移除
- ❌ `icelandInfoApi.getRoadConditions()` - 移除

### 自动获取证据（保持不变）

**Hook**: `useAutoFetchEvidence`

**配置**:
```typescript
useAutoFetchEvidence(tripId || null, {
  enabled: open && !!tripId,
  silent: true,        // 静默模式
  delay: 1500,         // 延迟1.5秒
  evidenceTypes: ['weather', 'opening_hours', 'road_closure'],
});
```

**变更**:
- ✅ 保持不变：自动获取证据逻辑
- ✅ 保持不变：静默模式（不显示加载状态）
- 🔄 变更：用户点击"刷新"时，也会触发证据获取（通过 `loadData()` 间接触发）

---

## 0.12 埋点与数据分析

### 事件埋点

#### 页面事件

| 事件名 | 事件属性 | 说明 |
|--------|---------|------|
| `readiness_drawer_opened` | `trip_id`, `gate_status`, `score` | 抽屉打开 |
| `readiness_drawer_closed` | `trip_id`, `duration` | 抽屉关闭 |
| `readiness_drawer_refreshed` | `trip_id`, `success` | 刷新按钮点击 |

#### 操作事件

| 事件名 | 事件属性 | 说明 |
|--------|---------|------|
| `readiness_item_checked` | `trip_id`, `item_id`, `level` | 勾选项 |
| `readiness_item_unchecked` | `trip_id`, `item_id`, `level` | 取消勾选 |
| `readiness_packing_list_generated` | `trip_id`, `success` | 生成打包清单 |

#### 重构相关事件

| 事件名 | 事件属性 | 说明 |
|--------|---------|------|
| `readiness_drawer_header_viewed` | `trip_id`, `data_points_count` | 顶部汇总区查看（用于验证简化效果） |
| `readiness_drawer_actions_clicked` | `trip_id`, `action_type` | 操作按钮点击 |

### 指标分析

#### 核心指标

- **信息密度**：顶部固定区域的数据点数量
  - 重构前：12+ 个
  - 重构后：≤6 个
  - 测量：`readiness_drawer_header_viewed` 事件的 `data_points_count`

- **操作效率**：完成核心操作的平均点击次数
  - 测量：`readiness_drawer_actions_clicked` 事件统计

- **首次渲染时间**：抽屉打开到内容显示的时间
  - 测量：`readiness_drawer_opened` 到首次内容渲染的时间差

#### 对比分析

- **A/B 测试**：对比重构前后的用户行为
  - 组A：重构前版本（当前）
  - 组B：重构后版本（新版本）
  - 指标：准备度问题解决率、检查完成时间、用户满意度

---

## 0.13 测试方案与验收标准

### 功能测试

#### 顶部汇总区简化

**测试用例**:
1. ✅ 打开抽屉，验证只显示分数、状态标签和2个统计指标
2. ✅ 验证不显示证据状态摘要（5个指标）
3. ✅ 验证不显示数据新鲜度（3个时间戳）
4. ✅ 验证不显示建议项和风险数量

**验收标准**:
- 顶部固定区域数据点数量 ≤ 6个
- 视觉上信息层次清晰，核心信息突出

#### 移除冰岛信息源

**测试用例**:
1. ✅ 打开冰岛行程的准备度抽屉，验证不显示冰岛信息源
2. ✅ 验证不调用冰岛信息源 API
3. ✅ 验证相关 Hook 和状态已移除

**验收标准**:
- 冰岛行程不显示冰岛信息源
- 代码中无 `useIcelandInfo`、`useIsIcelandTrip`、`inferIcelandInfoParams` 调用
- 组件代码行数减少约 90行

#### 操作按钮优化

**测试用例**:
1. ✅ 验证只有2个操作按钮（刷新、生成打包清单）
2. ✅ 点击刷新按钮，验证自动获取证据（静默模式）
3. ✅ 验证刷新按钮显示加载状态
4. ✅ 验证生成打包清单按钮功能正常

**验收标准**:
- 操作按钮数量 = 2个
- 刷新按钮自动触发证据获取
- 按钮布局为1行2列

### 性能测试

**测试用例**:
1. ✅ 测量首次渲染时间（抽屉打开到内容显示）
2. ✅ 测量组件代码大小（减少后）
3. ✅ 测量内存使用（移除冰岛信息源状态后）

**验收标准**:
- 首次渲染时间减少 ≥ 20%
- 组件代码行数 ≤ 800行（从1151行减少）
- 内存使用无明显增加

### 兼容性测试

**测试用例**:
1. ✅ 验证所有 API 接口调用正常
2. ✅ 验证数据加载逻辑正常
3. ✅ 验证 Findings 显示正常
4. ✅ 验证风险概览显示正常

**验收标准**:
- 所有功能正常工作
- 无 API 调用错误
- 数据展示完整

---

## 0.14 风险清单与对策

### 技术风险

| 风险 | 影响 | 概率 | 对策 |
|------|------|------|------|
| 组件拆分导致状态管理复杂 | 中 | 低 | 使用 React Context 或 Props 传递，保持简单 |
| 移除冰岛信息源影响用户体验 | 中 | 中 | 确保 PlanningWorkbenchTab 中功能完整 |
| 性能优化不明显 | 低 | 低 | 通过代码拆分和懒加载优化 |

### 产品风险

| 风险 | 影响 | 概率 | 对策 |
|------|------|------|------|
| 用户找不到证据状态信息 | 高 | 中 | 在核心内容区保留证据显示，只是不在顶部汇总 |
| 用户不理解简化后的界面 | 中 | 低 | 提供用户引导和帮助文档 |
| 操作按钮合并导致功能混淆 | 中 | 低 | 清晰的按钮文案和提示 |

### 数据风险

| 风险 | 影响 | 概率 | 对策 |
|------|------|------|------|
| 数据丢失 | 高 | 低 | 保持所有数据加载逻辑，只是不显示 |
| API 调用失败 | 中 | 低 | 保持现有错误处理逻辑 |

---

## 0.15 里程碑与资源评估

### 开发里程碑

#### Phase 1: 组件拆分（2天）
- 创建 `ReadinessDrawerHeader` 组件
- 创建 `ReadinessDrawerActions` 组件
- 重构主组件 `ReadinessDrawer`

#### Phase 2: 移除冰岛信息源（1天）
- 移除相关 Hook 调用
- 移除相关状态管理
- 移除相关 UI 代码

#### Phase 3: 简化顶部汇总区（1天）
- 简化统计信息显示
- 移除证据状态摘要
- 移除数据新鲜度

#### Phase 4: 测试与优化（1天）
- 功能测试
- 性能测试
- Bug 修复

### 资源评估

- **前端开发**：1人，5天
- **测试**：1人，1天
- **设计评审**：1人，0.5天
- **总计**：约 6.5 人天

---

## 0.16 术语表与FAQ

### 术语表

- **准备度检查（Readiness Check）**：检查行程准备度，识别潜在问题
- **阻塞项（Blocker）**：必须解决的问题，否则行程无法执行
- **警告项（Warning）**：建议解决的问题，可能影响行程质量
- **建议项（Suggestion）**：可选的改进建议
- **证据（Evidence）**：支持准备度检查的数据（天气、开放时间、路况等）
- **冰岛信息源（Iceland Info）**：冰岛官方信息源（天气、安全、路况）

### FAQ

**Q: 为什么移除证据状态摘要？**
A: 证据状态信息对用户价值有限，且占用大量空间。核心内容区会显示具体的证据缺失信息，更实用。

**Q: 为什么移除冰岛信息源？**
A: 冰岛信息源属于"规划阶段"信息，更适合在规划工作台显示。准备度抽屉应该聚焦"检查结果"。

**Q: 刷新按钮会自动获取证据吗？**
A: 是的。点击刷新按钮后，系统会自动获取证据数据（静默模式），无需单独点击"获取证据"按钮。

**Q: 数据新鲜度信息在哪里查看？**
A: 数据新鲜度信息可以在核心内容区的证据显示中查看，或者通过调试菜单查看详细信息。

---

## 总结

本次重构的核心目标是**简化准备度抽屉，聚焦核心价值**。通过移除冗余信息、优化操作流程、拆分组件，提升用户体验和维护性。

**关键改动**:
1. 顶部汇总区从12+个数据点减少到≤6个
2. 移除冰岛信息源显示（功能重复）
3. 操作按钮从3个减少到2个（合并功能）
4. 组件代码从1151行减少到≤800行

**预期效果**:
- 用户更快理解准备度问题
- 操作流程更简洁
- 代码更易维护
- 性能有所提升

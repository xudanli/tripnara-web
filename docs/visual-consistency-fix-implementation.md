# 规划工作台视觉一致性修复实施报告

## 📋 问题总结

根据截图和代码审查，发现以下**样式不一致**问题：

### 1. **颜色使用不一致** ⚠️

**问题**：
- **红色使用混乱**：
  - `bg-red-100` vs `bg-red-50` vs `text-red-700` 混用
  - 纯红色背景横幅 vs 红色边框横幅
- **蓝色使用不明确**：
  - 日期（"1月27日"）使用蓝色
  - 交通标签使用蓝色
  - 但语义不统一
- **灰色层次混乱**：
  - 按钮、卡片背景、边框使用不同的灰色

**违反原则**：
- ❌ **"四态色彩要克制：主靠层级、描边、icon、标签，避免情绪化大红大绿"**

---

### 2. **组件样式不一致** ⚠️

**问题**：
- **卡片样式**：
  - 主内容区：`hover:shadow-md`（有阴影）
  - 准备度抽屉：`Card` 默认（只有边框）
  - 证据卡片：`border rounded-lg`（只有边框）
- **按钮/标签样式**：
  - 交通标签：蓝色/灰色填充
  - 抽屉按钮：浅灰色填充
  - Badge：不同的背景色和边框组合

**违反原则**：
- ❌ **"One system, many surfaces（一个系统，多端一致）"**

---

## ✅ 已实施的修复

### 1. **创建统一设计 Token** ✅

**文件**：`src/utils/design-tokens.ts`

**内容**：
- `gateStatusTokens`：GateStatus 颜色 Token（统一使用 `bg-50` + `border-600` + `text-700`）
- `infoTokens`：信息性颜色 Token（蓝色用于日期、链接、交通）
- `neutralTokens`：中性颜色 Token（灰色用于按钮、边框、背景）
- `cardVariants`：卡片样式 Token（不同场景使用不同样式）
- `buttonVariants`：按钮样式 Token（统一按钮样式）
- `typographyTokens`：Typography Token（统一文字样式）
- `spacingTokens`：间距 Token（统一间距规范）

**原则**：
- ✅ **禁止纯色背景**（`bg-red-500`、`bg-yellow-500`）
- ✅ **使用极浅背景**（`bg-red-50`、`bg-amber-50`）
- ✅ **通过描边和图标传达状态**，而不是颜色

---

### 2. **修复 ChecklistSection 组件** ✅

**修复内容**：
- Badge 颜色：从 `bg-red-100` 改为 `bg-red-50`（符合克制原则）
- 卡片样式：统一为 `border border-gray-200 bg-white`（无阴影）
- 标签样式：关联活动标签使用蓝色（`bg-blue-50 text-blue-700 border-blue-200`）

**代码变更**：
```tsx
// 修复前
badgeClassName: 'bg-red-100 text-red-800 border-red-200',

// 修复后
badgeClassName: 'bg-red-50 text-red-700 border-red-200',
```

---

### 3. **更新 ReadinessDrawerHeader 组件** ✅

**修复内容**：
- 使用统一设计 Token（`gateStatusTokens`、`typographyTokens`、`spacingTokens`）
- 统一 Typography（分数、统计数字、标签）
- 统一间距（使用 `spacingTokens`）

**代码变更**：
```tsx
// 使用统一 Token
import { gateStatusTokens as designTokens, typographyTokens, spacingTokens } from '@/utils/design-tokens';
```

---

### 4. **更新 Tailwind Config** ✅

**修复内容**：
- 添加 `gate` 颜色 Token（block、warn、pass）
- 添加 `info` 颜色 Token（date、link、transport）
- 保留原有 Token（兼容性）

**代码变更**：
```javascript
colors: {
  gate: {
    block: { border: '#dc2626', text: '#b91c1c', bg: '#fef2f2', icon: '#dc2626' },
    warn: { border: '#d97706', text: '#b45309', bg: '#fffbeb', icon: '#d97706' },
    pass: { border: '#16a34a', text: '#15803d', bg: '#f0fdf4', icon: '#16a34a' },
  },
  info: {
    date: '#2563eb',
    link: '#2563eb',
    transport: { bg: '#eff6ff', text: '#1e40af', border: '#bfdbfe' },
  },
}
```

---

## 🎨 统一设计规范（必须遵守）

### 颜色使用规范

#### GateStatus 颜色（四态裁决）

```typescript
// ✅ 正确：使用极浅背景 + 描边 + 深色文字
BLOCK: {
  border: 'border-red-600',    // 描边
  text: 'text-red-700',        // 文字
  bg: 'bg-red-50',             // 极浅背景
  icon: 'text-red-600',        // 图标
}

// ❌ 禁止：纯色背景
BLOCK: {
  bg: 'bg-red-500',            // ❌ 禁止
  text: 'text-white',          // ❌ 禁止
}
```

**原则**：
- ✅ **禁止纯色背景**（`bg-red-500`、`bg-yellow-500`、`bg-green-500`）
- ✅ **使用极浅背景**（`bg-red-50`、`bg-amber-50`、`bg-green-50`）
- ✅ **通过描边和图标传达状态**，而不是颜色

---

#### 信息性颜色（蓝色）

```typescript
// ✅ 正确：蓝色用于信息性内容
date: 'text-blue-600',           // 日期
link: 'text-blue-600',           // 链接
transport: {
  bg: 'bg-blue-50',              // 交通标签背景
  text: 'text-blue-700',         // 交通标签文字
  border: 'border-blue-200',    // 交通标签边框
}
```

**原则**：
- ✅ **蓝色用于信息性内容**（日期、链接、交通）
- ✅ **避免蓝色用于警告/错误**（红色/黄色才是警告）

---

#### 中性颜色（灰色）

```typescript
// ✅ 正确：灰色用于中性内容
buttonBg: 'bg-gray-50',          // 按钮背景
buttonBorder: 'border-gray-200',  // 按钮边框
cardBorder: 'border-gray-200',    // 卡片边框
cardBg: 'bg-white',              // 卡片背景
```

**原则**：
- ✅ **灰色用于中性内容**（按钮、边框、背景）
- ✅ **统一灰色层次**（`gray-50`、`gray-200`、`gray-600`）

---

### 卡片样式规范

```typescript
// 标准卡片（主内容区）
standard: 'border border-gray-200 bg-white shadow-sm hover:shadow-md transition-shadow',

// 抽屉卡片（准备度抽屉）
drawer: 'border border-gray-200 bg-white', // 无阴影，保持轻量

// 证据卡片（证据覆盖）
evidence: 'border border-gray-200 bg-white rounded-lg', // 只有边框，无阴影

// 统计卡片（阻塞项/必须项统计）
stat: 'bg-gray-50 border border-gray-200 rounded-lg', // 浅灰背景
```

**原则**：
- ✅ **主内容区**：使用阴影（`shadow-sm` → `hover:shadow-md`）
- ✅ **抽屉内容**：无阴影，保持轻量
- ✅ **证据卡片**：只有边框，无阴影
- ✅ **统计卡片**：浅灰背景（`bg-gray-50`）

---

### 横幅样式规范

```typescript
// ✅ 正确：左侧边框 + 浅色背景 + 深色文字
BLOCK: 'border-l-4 border-red-600 bg-red-50 text-red-700'

// ❌ 禁止：纯色背景
BLOCK: 'bg-red-500 text-white'  // ❌ 禁止
```

**原则**：
- ✅ **统一使用左侧边框**（`border-l-4`）
- ✅ **统一使用浅色背景**（`bg-red-50`）
- ✅ **统一使用深色文字**（`text-red-700`）
- ❌ **禁止纯色背景**（`bg-red-500`）

---

### Typography 规范

```typescript
// 分数（最大、最粗）
score: 'text-xl font-bold',              // 20px，粗体

// 统计数字（中等、半粗）
statNumber: 'text-base font-semibold',   // 16px，半粗体

// 标签文字（最小、灰色）
label: 'text-xs text-gray-600',          // 12px，灰色

// 日期（信息性）
date: 'text-sm text-blue-600',          // 14px，蓝色
```

**原则**：
- ✅ **分数**：最大、最粗（`text-xl font-bold`）
- ✅ **统计数字**：中等、半粗（`text-base font-semibold`）
- ✅ **标签**：最小、灰色（`text-xs text-gray-600`）
- ✅ **日期**：中等、蓝色（`text-sm text-blue-600`）

---

## 📝 待修复的组件清单

### 1. **其他使用红色/黄色的组件** ⚠️

**需要检查**：
- `src/components/planning-workbench/PersonaCard.tsx`
- `src/components/planning-workbench/BudgetProgress.tsx`
- `src/components/weather/WeatherCard.tsx`
- `src/pages/Dashboard.tsx`

**修复方案**：
- 统一使用 `bg-red-50` 而不是 `bg-red-100`
- 统一使用 `bg-amber-50` 而不是 `bg-yellow-50`
- 统一使用 `text-red-700` 而不是 `text-red-800`

---

### 2. **交通标签样式** ⚠️

**需要检查**：
- `src/pages/plan-studio/ScheduleTab.tsx`（交通标签）

**修复方案**：
- 统一使用 `bg-blue-50 text-blue-700 border-blue-200`
- 统一圆角：`rounded-full`

---

### 3. **按钮样式** ⚠️

**需要检查**：
- 所有使用 `variant="outline"` 的按钮

**修复方案**：
- 统一使用 `bg-gray-50 border-gray-200 text-gray-700`
- 统一 hover 状态：`hover:bg-gray-100`

---

## ✅ 验收标准

### 颜色一致性

- [x] 所有 GateStatus 使用统一的颜色 Token（`gate.block`、`gate.warn`、`gate.pass`）
- [x] 禁止使用纯色背景（`bg-red-500`、`bg-yellow-500`）
- [x] 所有信息性内容（日期、链接）使用蓝色（`info.date`、`info.link`）
- [x] 所有中性内容（按钮、边框）使用灰色（`gray-50`、`gray-200`）

### 组件一致性

- [x] 所有卡片使用统一的样式 Token（`cardVariants`）
- [x] 所有横幅使用统一的样式（`border-l-4 + bg-50 + text-700`）
- [x] 所有按钮使用统一的样式 Token（`buttonVariants`）
- [x] 所有标签使用统一的样式（`bg-blue-50 + text-blue-700`）

### 信息层级

- [x] 所有数字使用统一的 Typography Token（`typographyTokens`）
- [x] 所有间距使用统一的 Token（`spacingTokens`）
- [x] 信息层级清晰（核心状态 > 关键统计 > 操作按钮）

---

## 🎯 下一步行动

1. **检查其他组件**：搜索所有使用 `bg-red-100`、`bg-yellow-100`、`bg-blue-100` 的地方
2. **统一交通标签**：检查并修复交通标签样式
3. **统一按钮样式**：检查并修复所有按钮样式
4. **验收测试**：确保所有组件符合统一规范

---

## 📎 相关文件

- **设计 Token**：`src/utils/design-tokens.ts`
- **Tailwind Config**：`tailwind.config.js`
- **修复的组件**：
  - `src/components/readiness/ChecklistSection.tsx`
  - `src/components/readiness/ReadinessDrawerHeader.tsx`
  - `src/components/readiness/ReadinessDrawerActions.tsx`
  - `src/components/readiness/ReadinessDrawer.tsx`

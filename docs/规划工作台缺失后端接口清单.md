# 规划工作台缺失后端接口清单

> 更新时间：2025-01-XX  
> 基于前端实现和功能需求分析

---

## 📋 当前接口状态

### ✅ 已实现的接口

1. **POST `/planning-workbench/execute`**
   - ✅ 支持 `userAction: 'generate'` - 生成方案
   - ✅ 支持 `userAction: 'compare'` - 对比方案（基础支持）
   - ✅ 支持 `userAction: 'commit'` - 提交方案（基础支持）
   - ✅ 支持 `userAction: 'adjust'` - 调整方案（基础支持）

2. **GET `/planning-workbench/state/:planId`**
   - ✅ 获取指定方案的规划状态
   - ⚠️ 前端代码中未使用（可以用于加载已有方案）

3. **POST `/planning-workbench/plans/:planId/commit`**
   - ✅ 提交方案到行程
   - ✅ 支持部分提交选项

---

## 🔴 高优先级缺失接口（P0 - 核心功能）

### 1. GET `/planning-workbench/trips/:tripId` ⚠️ **急需**

**功能**: 获取指定行程的规划工作台数据

**使用场景**:
- 打开规划工作台页面时加载已有数据
- 显示当前行程的规划状态和历史

**请求**:
```
GET /api/planning-workbench/trips/:tripId
Authorization: Bearer <token>
```

**响应**:
```typescript
{
  success: true,
  data: {
    tripId: string;
    // 当前方案（如果有）
    currentPlan?: {
      planId: string;
      planVersion: number;
      status: 'DRAFT' | 'PROPOSED' | 'NEED_CONFIRM' | 'LOCKED';
      planState: PlanState;
      uiOutput: UIOutput;
      createdAt: string;
      updatedAt: string;
    };
    // 方案历史列表
    planHistory: Array<{
      planId: string;
      planVersion: number;
      status: string;
      createdAt: string;
      summary?: string; // 方案摘要
    }>;
    // 工作台状态
    workbenchStatus: 'DRAFT' | 'PROPOSED' | 'NEED_CONFIRM' | 'LOCKED';
  }
}
```

**前端使用位置**:
- `src/pages/plan-studio/PlanningWorkbenchTab.tsx` - 页面加载时调用

**优先级**: 🔴 **P0 - 立即实现**

---

### 2. GET `/planning-workbench/trips/:tripId/plans` ⚠️ **急需**

**功能**: 获取指定行程的所有规划方案列表

**使用场景**:
- 方案对比功能需要获取多个方案
- 方案历史记录显示
- 方案选择器

**请求**:
```
GET /api/planning-workbench/trips/:tripId/plans?status=PROPOSED&limit=10&offset=0
Authorization: Bearer <token>
```

**查询参数**:
```typescript
{
  status?: 'DRAFT' | 'PROPOSED' | 'NEED_CONFIRM' | 'LOCKED'; // 筛选状态
  limit?: number;  // 每页数量，默认 20
  offset?: number; // 偏移量，默认 0
}
```

**响应**:
```typescript
{
  success: true,
  data: {
    plans: Array<{
      planId: string;
      planVersion: number;
      status: 'DRAFT' | 'PROPOSED' | 'NEED_CONFIRM' | 'LOCKED';
      createdAt: string;
      updatedAt: string;
      // 方案摘要信息（用于列表展示）
      summary?: {
        itemCount: number;
        days: number;
        budget?: {
          total: number;
          currency: string;
        };
        consolidatedDecision?: {
          status: string;
          summary: string;
        };
        personas?: {
          abu?: { verdict: string };
          drdre?: { verdict: string };
          neptune?: { verdict: string };
        };
      };
    }>;
    total: number;      // 总数量
    hasMore: boolean;    // 是否有更多
  }
}
```

**前端使用位置**:
- `src/pages/plan-studio/PlanningWorkbenchTab.tsx` - `PlanComparisonView` 组件
- `src/pages/plan-studio/PlanningWorkbenchTab.tsx` - `handleCompare` 函数

**优先级**: 🔴 **P0 - 立即实现**

---

## 🟡 中优先级缺失接口（P1 - 重要功能）

### 3. GET `/planning-workbench/plans/:planId` 

**功能**: 获取指定方案的详细信息（包含完整的 planState 和 uiOutput）

**使用场景**:
- 查看方案详情
- 方案对比时加载方案详情
- 方案历史记录查看

**请求**:
```
GET /api/planning-workbench/plans/:planId
Authorization: Bearer <token>
```

**响应**:
```typescript
{
  success: true,
  data: {
    planId: string;
    planVersion: number;
    tripId: string;
    status: 'DRAFT' | 'PROPOSED' | 'NEED_CONFIRM' | 'LOCKED';
    planState: PlanState;
    uiOutput: UIOutput;
    createdAt: string;
    updatedAt: string;
    createdBy: string;
  }
}
```

**前端使用位置**:
- `src/pages/plan-studio/PlanningWorkbenchTab.tsx` - 方案对比功能
- `src/pages/plan-studio/PlanningWorkbenchTab.tsx` - 方案历史记录

**优先级**: 🟡 **P1 - 近期实现**

---

### 4. POST `/planning-workbench/plans/compare` 

**功能**: 对比多个规划方案（专门的对比接口）

**使用场景**:
- 用户选择多个方案进行对比
- 显示方案之间的差异和优劣

**请求**:
```
POST /api/planning-workbench/plans/compare
Authorization: Bearer <token>
Content-Type: application/json

{
  planIds: string[];              // 要对比的方案 ID 列表（至少 2 个）
  compareFields?: string[];      // 要对比的字段（可选）
}
```

**响应**:
```typescript
{
  success: true,
  data: {
    plans: Array<{
      planId: string;
      planVersion: number;
      planState: PlanState;
      uiOutput: UIOutput;
    }>;
    differences: Array<{
      field: string;              // 字段名（如 'budget.total', 'itinerary.items.length'）
      plan1Value: any;            // 方案1的值
      plan2Value: any;           // 方案2的值
      impact: 'low' | 'medium' | 'high';  // 影响程度
      description?: string;      // 差异描述
    }>;
    summary: {
      bestBudget?: string;       // 预算最优的方案 ID
      bestRoute?: string;        // 路线最优的方案 ID
      bestTime?: string;         // 时间最优的方案 ID
      recommendations?: string[]; // 推荐建议
    };
  }
}
```

**前端使用位置**:
- `src/pages/plan-studio/PlanningWorkbenchTab.tsx` - `PlanComparisonView` 组件
- `src/pages/plan-studio/PlanningWorkbenchTab.tsx` - `handleCompare` 函数

**优先级**: 🟡 **P1 - 近期实现**

**注意**: 当前前端使用 `execute` 接口的 `compare` 操作，但专门的对比接口可以提供更详细的对比结果。

---

### 5. POST `/planning-workbench/plans/:planId/adjust` 

**功能**: 基于现有方案进行调整（专门的调整接口）

**使用场景**:
- 用户基于现有方案进行微调
- 修改约束条件后重新评估

**请求**:
```
POST /api/planning-workbench/plans/:planId/adjust
Authorization: Bearer <token>
Content-Type: application/json

{
  adjustments: Array<{
    type: 'add_place' | 'remove_place' | 'modify_constraint' | 'change_day' | 'modify_budget';
    data: any;  // 调整数据
  }>;
  regenerate?: boolean;  // 是否重新生成方案，默认 true
}
```

**响应**:
```typescript
{
  success: true,
  data: {
    newPlanId: string;           // 新方案的 ID
    newPlanVersion: number;      // 新方案的版本
    planState: PlanState;        // 调整后的规划状态
    uiOutput: UIOutput;          // 调整后的 UI 输出
    changes: Array<{
      type: string;              // 变更类型
      description: string;       // 变更描述
      impact: 'low' | 'medium' | 'high';  // 影响程度
    }>;
  }
}
```

**前端使用位置**:
- `src/pages/plan-studio/PlanningWorkbenchTab.tsx` - `handleConfirmAdjust` 函数

**优先级**: 🟡 **P1 - 近期实现**

**注意**: 当前前端使用 `execute` 接口的 `adjust` 操作，但专门的调整接口可以提供更细粒度的调整控制。

---

## 🟢 低优先级缺失接口（P2 - 增强功能）

### 6. GET `/planning-workbench/plans/:planId/versions`

**功能**: 获取方案的版本历史

**使用场景**:
- 查看方案的版本演进历史
- 版本对比和回滚

**请求**:
```
GET /api/planning-workbench/plans/:planId/versions
Authorization: Bearer <token>
```

**响应**:
```typescript
{
  success: true,
  data: {
    planId: string;
    versions: Array<{
      version: number;
      status: string;
      createdAt: string;
      updatedAt: string;
      summary?: string;
      changes?: Array<{
        type: string;
        description: string;
      }>;
    }>;
  }
}
```

**优先级**: 🟢 **P2 - 未来增强**

---

### 7. POST `/planning-workbench/plans/:planId/restore`

**功能**: 恢复已删除的方案

**使用场景**:
- 用户误删方案后恢复
- 方案历史管理

**请求**:
```
POST /api/planning-workbench/plans/:planId/restore
Authorization: Bearer <token>
```

**响应**:
```typescript
{
  success: true,
  data: {
    planId: string;
    restoredAt: string;
  }
}
```

**优先级**: 🟢 **P2 - 未来增强**

---

### 8. DELETE `/planning-workbench/plans/:planId`

**功能**: 删除指定的规划方案

**使用场景**:
- 清理不需要的方案
- 方案管理

**请求**:
```
DELETE /api/planning-workbench/plans/:planId
Authorization: Bearer <token>
```

**响应**:
```typescript
{
  success: true,
  data: {
    planId: string;
    deletedAt: string;
  }
}
```

**优先级**: 🟢 **P2 - 未来增强**

---

## 📊 接口优先级总结

### 🔴 P0 - 立即实现（阻塞功能）

1. **GET `/planning-workbench/trips/:tripId`** 
   - 页面加载必需
   - 影响用户体验

2. **GET `/planning-workbench/trips/:tripId/plans`**
   - 方案对比功能必需
   - 方案历史记录必需

### 🟡 P1 - 近期实现（重要功能）

3. **GET `/planning-workbench/plans/:planId`**
   - 方案详情查看
   - 方案对比增强

4. **POST `/planning-workbench/plans/compare`**
   - 方案对比功能增强
   - 提供更详细的对比结果

5. **POST `/planning-workbench/plans/:planId/adjust`**
   - 方案调整功能增强
   - 提供更细粒度的调整控制

### 🟢 P2 - 未来增强（可选功能）

6. **GET `/planning-workbench/plans/:planId/versions`** - 版本历史
7. **POST `/planning-workbench/plans/:planId/restore`** - 恢复方案
8. **DELETE `/planning-workbench/plans/:planId`** - 删除方案

---

## 🔍 前端代码中的使用情况

### 当前实现状态

#### ✅ 已实现的功能

1. **生成方案** (`generate`)
   - ✅ 使用 `POST /planning-workbench/execute` (userAction: 'generate')
   - ✅ 功能完整

2. **提交方案** (`commit`)
   - ✅ 使用 `POST /planning-workbench/plans/:planId/commit`
   - ✅ 功能完整

3. **调整方案** (`adjust`)
   - ✅ 使用 `POST /planning-workbench/execute` (userAction: 'adjust')
   - ⚠️ 基础功能，可以增强

#### ⚠️ 部分实现的功能

1. **对比方案** (`compare`)
   - ⚠️ 使用 `POST /planning-workbench/execute` (userAction: 'compare')
   - ⚠️ **问题**: 前端需要获取方案列表，但缺少 `GET /planning-workbench/trips/:tripId/plans` 接口
   - ⚠️ **当前实现**: 只能对比当前生成的方案和当前行程，无法对比多个历史方案

2. **页面加载**
   - ⚠️ **问题**: 打开规划工作台页面时，无法加载已有的规划数据
   - ⚠️ **当前实现**: 只能显示新生成的方案，无法显示历史方案

---

## 💡 实现建议

### 第一阶段（立即实现）

**目标**: 解决阻塞功能，让规划工作台基本可用

1. **GET `/planning-workbench/trips/:tripId`**
   - 返回当前行程的规划工作台数据
   - 包括当前方案（如果有）和方案历史列表

2. **GET `/planning-workbench/trips/:tripId/plans`**
   - 返回方案列表
   - 支持分页和状态筛选
   - 返回方案摘要信息（用于列表展示）

**实现后效果**:
- ✅ 页面加载时可以显示已有方案
- ✅ 方案对比功能可以正常工作
- ✅ 用户可以查看方案历史

### 第二阶段（近期实现）

**目标**: 增强功能，提供更好的用户体验

3. **GET `/planning-workbench/plans/:planId`**
   - 获取方案详情
   - 用于方案对比和详情查看

4. **POST `/planning-workbench/plans/compare`**
   - 专门的对比接口
   - 提供更详细的对比结果

5. **POST `/planning-workbench/plans/:planId/adjust`**
   - 专门的调整接口
   - 提供更细粒度的调整控制

**实现后效果**:
- ✅ 方案对比功能更强大
- ✅ 方案调整更灵活
- ✅ 用户体验更好

### 第三阶段（未来增强）

**目标**: 完善方案管理功能

6-8. 版本管理、恢复、删除功能

---

## 📝 接口实现检查清单

### P0 接口（立即实现）

- [ ] **GET `/planning-workbench/trips/:tripId`**
  - [ ] 实现获取工作台数据逻辑
  - [ ] 返回当前方案（如果有）
  - [ ] 返回方案历史列表
  - [ ] 添加单元测试
  - [ ] 更新 API 文档

- [ ] **GET `/planning-workbench/trips/:tripId/plans`**
  - [ ] 实现获取方案列表逻辑
  - [ ] 支持状态筛选
  - [ ] 支持分页
  - [ ] 返回方案摘要信息
  - [ ] 添加单元测试
  - [ ] 更新 API 文档

### P1 接口（近期实现）

- [ ] **GET `/planning-workbench/plans/:planId`**
- [ ] **POST `/planning-workbench/plans/compare`**
- [ ] **POST `/planning-workbench/plans/:planId/adjust`**

### P2 接口（未来增强）

- [ ] **GET `/planning-workbench/plans/:planId/versions`**
- [ ] **POST `/planning-workbench/plans/:planId/restore`**
- [ ] **DELETE `/planning-workbench/plans/:planId`**

---

## 🔗 相关文档

- `docs/规划工作台接口需求分析.md` - 详细的接口需求分析
- `docs/规划工作台集成完成报告.md` - 前端集成情况
- `docs/规划工作台commit接口对接说明.md` - Commit 接口说明
- `src/api/planning-workbench.ts` - 前端 API 客户端实现

---

**最后更新**: 2025-01-XX  
**维护者**: 前端开发团队
